# job_finder/assets — static assets package.
#
# Importable via importlib.resources so asset files can be resolved
# regardless of where the package is installed:
#
#   from importlib import resources
#   with resources.files("job_finder.assets").joinpath("tray_icon.png").open("rb") as fh:
#       ...
