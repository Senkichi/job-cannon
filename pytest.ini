[pytest]
markers =
    e2e: end-to-end browser tests (require Playwright)
    integration: end-to-end integration tests requiring live external services (Ollama, network APIs); opt-in via `pytest -m integration`
addopts = -m "not integration"
