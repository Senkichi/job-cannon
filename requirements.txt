# Core
google-api-python-client~=2.100
google-auth-httplib2~=0.2
google-auth-oauthlib~=1.2
google-genai>=1.0.0
beautifulsoup4~=4.12
pyyaml~=6.0
requests~=2.31

# Web
Flask~=3.1
APScheduler>=3.11,<4.0
psutil>=5.9,<7    # Cross-process PID liveness check for scheduler pidfile lock
anthropic~=0.84

# Scoring
thefuzz[speedup]~=0.22  # fuzzy string matching (formerly fuzzywuzzy)

# Multi-provider routing
jsonschema>=4.0.0

# Phase 33 shootout statistics — paired MAE + BCa bootstrap 95% CIs
numpy~=2.4
scipy~=1.17


# Optional: JSearch
# (uses requests directly, no extra package)

# Telemetry — installed separately: uv pip install -e ../anthropic-telemetry

# Content extraction
trafilatura~=2.0             # Density-based main-content extraction (strips chrome/nav/aggregator boilerplate)

# Search / discovery — used by enrichment_tiers + agentic_enricher to look
# up career pages when neither a known ATS slug nor a sitemap-derived URL
# is available.
ddgs~=9.12

# Dev
pytest~=8.0
pytest-playwright~=0.7
python-dotenv~=1.0
