# Auth & secrets
credentials.json
token.json
.env

# User data (personal profile, config with API keys, resume artifacts)
config.yaml
experience_profile.json
experience_reference.md
resume_style_guide.json

# Database
*.db

# Python
__pycache__/
*.pyc
.venv/
*.egg-info/

# Reports
reports/
eval_results/

# IDE
.vscode/
.idea/

# Application logs
logs/
test_output.txt

# Backups
backups/

# Application data (parse failure archives, resume uploads)
data/

# Development tooling (not for distribution)
.claude/
AUDIT_LOG.md

# Code review / audit tool working directories
.nit/
.nit-supreme/
.superpowers/
.playwright-mcp/

# Archived planning docs (kept on disk for reference, not for distribution)
docs/superpowers/

# Screenshots from exploration/testing sessions
*.png

# SQLite WAL artifacts
*.db-shm
*.db-wal
*.db.bak*

# Internal planning + working notes — NEVER tracked, with one exception.
/.planning/

# Eval-harness baseline reports — tracked as portfolio artifacts. The
# eval harness writes new reports here by default; only files explicitly
# checked in are version-controlled.
!/.planning/eval_results/
!/.planning/eval_results/**
