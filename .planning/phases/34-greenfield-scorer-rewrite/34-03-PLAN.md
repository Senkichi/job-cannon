---
phase: 34-greenfield-scorer-rewrite
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - job_finder/db.py
  - job_finder/web/exclusion_filter.py
  - job_finder/web/careers_crawler.py
  - job_finder/web/ats_scanner.py
  - job_finder/web/agentic_enricher.py
  - job_finder/web/blueprints/companies.py
  - job_finder/web/dedup_normalizer.py
  - job_finder/web/rejection_analyzer.py
  - job_finder/web/rejection_patterns.py
  - job_finder/web/blueprints/batch_scoring.py
  - job_finder/web/blueprints/dashboard.py
  - job_finder/web/blueprints/resume.py
  - job_finder/web/templates/jobs/_row_detail.html
  - job_finder/web/templates/jobs/_row_expanded.html
  - job_finder/web/templates/jobs/_score_cell.html
  - job_finder/web/templates/jobs/_resume_section.html
  - job_finder/web/templates/costs/index.html
  - job_finder/web/resume_generator.py
  - job_finder/web/pipeline_runner.py
  - tests/test_careers_crawler.py
  - tests/test_ats_scanner.py
  - tests/test_rejection_patterns.py
  - tests/test_rejection_analyzer.py
  - tests/test_batch_scoring.py
  - tests/test_ingestion.py
  - tests/test_views.py
  - tests/test_resume.py
  - tests/test_dedup_normalizer.py
  - tests/test_data_enricher.py
  - tests/test_costs.py
  - tests/test_claude_client.py
  - tests/test_agentic_enricher.py
autonomous: true
requirements:
  - CONSUMERS-01
  - CONSUMERS-02
  - CONSUMERS-03
  - CONSUMERS-04
  - CONSUMERS-05
  - CONSUMERS-06
  - CONSUMERS-07
  - CONSUMERS-08
  - CONSUMERS-09
  - CONSUMERS-10
  - CONSUMERS-11
  - CONSUMERS-12
  - CONSUMERS-13
  - CONSUMERS-14
  - CONSUMERS-15
  - CONSUMERS-16
  - TESTS-08
  - TESTS-09
  - TESTS-10
  - TESTS-11
  - TESTS-12
  - TESTS-13
  - TESTS-14
  - TESTS-19
  - TESTS-20

must_haves:
  truths:
    - "Ships as 5 independently-revertable commits (A→E) in dependency order; writes stay dual via Plan 2 shim THROUGHOUT Plan 3 — at no point during Plan 3 are legacy columns stale"
    - "Commit A (query-layer): db.py, exclusion_filter.py, careers_crawler.py, agentic_enricher.py, blueprints/companies.py, dedup_normalizer.py, rejection_analyzer.py, rejection_patterns.py switched to read from classification + sub_scores_json + fit_analysis rationale (ARCHITECTURE.md D-17)"
    - "Commit B (batch_scoring.py merge): batch_score_haiku_start + batch_score_sonnet_start merge into batch_score_start; _run_batch_haiku_bg + _run_batch_sonnet_bg merge into _run_batch_bg; session_type enum collapses from {haiku, sonnet, sync} to {scoring, sync}"
    - "Commit C (dashboard.py): single scoring_eligible_count replaces separate Haiku/Sonnet counts; _cached_tier_available('scoring') replaces tier_available('haiku')/('sonnet')"
    - "Commit D (templates): _row_detail.html, _row_expanded.html, _score_cell.html, _resume_section.html, costs/index.html swap numeric-score display to classification-enum badges + sub-score breakdown (numeric colored for apply=green/consider=amber/skip=gray/reject=red)"
    - "Commit E (resume/interview gating + pipeline summary keys): resume_generator.py sonnet_score >= multi_threshold → classification == 'apply'; pipeline_runner.run_ingestion summary keys haiku_scored/sonnet_queued/sonnet_evaluated collapse to scored + classified_* (legacy branch deleted)"
    - "ORDER BY classification uses a rank CASE expression (apply=4, consider=3, skip=2, reject=1) + sub_score_sum tiebreak — not raw enum string ordering"
    - "URL parameter sort-by still accepts legacy param names ('haiku_score', 'sonnet_score') during Plan 3 for backward-compat bookmarks; translates to classification + sub_score_sum; Plan 4 removes URL compat"
    - "No test file left in a temporarily-broken state between commits — each of A/B/C/D/E produces a green test suite independently"
    - "uv run --active pytest -q --tb=short exits 0 at every commit boundary within Plan 3"
  artifacts:
    - path: "job_finder/db.py"
      provides: "get_filtered_jobs() uses CASE-based classification-rank + sub_scores_json sum for ordering; min_score/max_score filter replaced by classification IN (…) predicate; allowed_sort_cols drops haiku_score/sonnet_score"
      contains: "classification"
    - path: "job_finder/web/exclusion_filter.py"
      provides: "count_scorable() replaces count_haiku_scorable(); predicate classification IS NULL"
      contains: "count_scorable"
    - path: "job_finder/web/blueprints/batch_scoring.py"
      provides: "Single batch_score_start route + _run_batch_bg background; session_type enum {scoring, sync}"
      contains: "batch_score_start"
    - path: "job_finder/web/templates/jobs/_row_detail.html"
      provides: "Classification badge + 6-dimension sub-score display (radar or bars — Claude's discretion)"
      contains: "classification"
  key_links:
    - from: "Templates (Jinja2 score_cell, row_detail, row_expanded)"
      to: "job row dict fields: classification, sub_scores_json, fit_analysis"
      via: "Jinja2 variable access: {{ job.classification }}, {{ job.sub_scores_json|from_json }}"
      pattern: "job\\.classification|job\\.sub_scores_json"
    - from: "get_filtered_jobs sort-by"
      to: "URL ?sort=classification|sub_score_sum"
      via: "Python allowlist check + SQL interpolation (classification_rank CASE expression)"
      pattern: "allowed_sort_cols"
    - from: "pipeline_runner.run_ingestion summary"
      to: "dashboard stats"
      via: "summary['scored'] + summary['classified_*'] keys read by dashboard blueprint"
      pattern: "classified_apply|classified_consider|classified_skip|classified_reject"
---

<objective>
Migrate every downstream read consumer from `haiku_score`/`sonnet_score`/`haiku_summary` to `classification` + `sub_scores_json` + `fit_analysis` (rationale) in five independently-revertable commits. The Plan 2 dual-write shim keeps legacy columns fresh throughout Plan 3, so every commit below can be individually reverted without leaving stale data. Plan 4 removes the shim (and deletes haiku_scorer.py etc); Plan 3 must finish first.

Purpose: ARCHITECTURE.md lines 289-301 mandate this ordering: read swaps must precede write removal. Templates sort-by-haiku_score via URL parameter would 500 if the query allowlist dropped haiku_score first — the commit atomicity of each of A/B/C/D/E is chosen to avoid any such cross-layer breakage. Each commit crosses exactly one semantic boundary (query layer, batch-scoring routes, dashboard, templates, resume/summary).

Output: 5 commits (A-E), each green-test. Every consumer in the ARCHITECTURE.md data-flow table (lines 120-141) migrated. Templates display classification-enum badges + per-dimension sub-score bars instead of numeric 0-100 scores.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/phases/34-greenfield-scorer-rewrite/34-CONTEXT.md
@.planning/phases/34-greenfield-scorer-rewrite/34-02-PLAN.md
@.planning/research/ARCHITECTURE.md
@.planning/REQUIREMENTS.md
@job_finder/db.py
@job_finder/web/exclusion_filter.py
@job_finder/web/careers_crawler.py
@job_finder/web/ats_scanner.py
@job_finder/web/agentic_enricher.py
@job_finder/web/blueprints/companies.py
@job_finder/web/blueprints/dashboard.py
@job_finder/web/blueprints/batch_scoring.py
@job_finder/web/blueprints/resume.py
@job_finder/web/dedup_normalizer.py
@job_finder/web/rejection_analyzer.py
@job_finder/web/rejection_patterns.py
@job_finder/web/resume_generator.py
@job_finder/web/pipeline_runner.py
@job_finder/web/templates/jobs/_row_detail.html
@job_finder/web/templates/jobs/_row_expanded.html
@job_finder/web/templates/jobs/_score_cell.html
@job_finder/web/templates/jobs/_resume_section.html
@job_finder/web/templates/costs/index.html

<interfaces>
<!-- Extracted migration targets from ARCHITECTURE.md lines 120-149 — authoritative file:line list. -->

## Query-layer targets (Commit A)

| File | Line | Current | v3.0 replacement |
|------|------|---------|------------------|
| careers_crawler.py | 625 | `WHERE j.haiku_score >= ?` | `WHERE j.classification IN ('apply','consider')` |
| careers_crawler.py | 559-703, 1258 | `summary["haiku_scored"]`, `summary["sonnet_evaluated"]` | Collapse to summary["scored"] + classification counts |
| ats_scanner.py | 675-1100 | Same counter block | Same collapse |
| agentic_enricher.py | 500 | `ORDER BY haiku_score DESC NULLS LAST` | `ORDER BY classification_rank DESC, sub_score_sum DESC` |
| blueprints/companies.py | 141, 144, 292-293 | `COALESCE(sonnet_score, haiku_score, score) as effective_score` | Classification-rank + sub_score_sum |
| exclusion_filter.py | 90 | `count_haiku_scorable()` pred `haiku_score IS NULL` | `count_scorable()` pred `classification IS NULL` |
| dedup_normalizer.py | 287-394 | UPDATE/merge over haiku_score/sonnet_score/fit_analysis | classification priority merge + element-wise max sub_scores |
| rejection_analyzer.py | 134-173 | SELECT haiku_score, sonnet_score, fit_analysis | Update column list; adapt prompt |
| rejection_patterns.py | 24-200 | RejectionPattern fields haiku_score/sonnet_score | classification / sub_scores fields |
| db.py | get_filtered_jobs() 472-492, 540-545 | COALESCE(sonnet_score, haiku_score, score) + min_score/max_score filter + allowed_sort_cols | classification-rank CASE + classification IN filter + updated allowlist |

## Batch scoring (Commit B)

| File | Lines | Current | v3.0 replacement |
|------|-------|---------|------------------|
| batch_scoring.py | 21-72, 263-359 | batch_score_haiku_start + _run_batch_haiku_bg | Merge into batch_score_start + _run_batch_bg |
| batch_scoring.py | 74-129, 361-442 | batch_score_sonnet_start + _run_batch_sonnet_bg | DELETE (merged above) |
| batch_scoring.py | 157, 249 | "Haiku" if session_type=="haiku" else "Sonnet" | "Scoring" label |
| batch_scoring.py | 88, 287, 386-387 | haiku_score IS NOT NULL AND haiku_score >= ? predicates | classification IN (…) predicates |

## Dashboard (Commit C)

| File | Lines | Current | v3.0 replacement |
|------|-------|---------|------------------|
| dashboard.py | 180-194 | Separate Haiku/Sonnet eligible counts; _cached_tier_available("haiku"/"sonnet") | Single scoring_eligible_count; _cached_tier_available("scoring") |

## Templates (Commit D)

| File | Lines | Current | v3.0 replacement |
|------|-------|---------|------------------|
| _row_detail.html | 6, 67, 109-149 | job.sonnet_score / job.haiku_score / job.haiku_summary | job.classification + job.sub_scores_json|from_json |
| _row_expanded.html | 45, 104-213 | Same | Same — score badge logic changes numeric → enum-colored |
| _score_cell.html | 9 | Same | Same |
| _resume_section.html | 54, 80 | Reference sonnet_score | Reference classification |
| costs/index.html | 148 | Remove haiku_score legend key | Add scoring legend entry |

## Resume gating + summary (Commit E)

| File | Line | Current | v3.0 replacement |
|------|------|---------|------------------|
| resume_generator.py | 415-430 | sonnet_score >= multi_threshold | classification == 'apply' |
| pipeline_runner.py | run_ingestion | Legacy else-branch with haiku_scored / sonnet_queued / sonnet_evaluated summary keys | DELETE legacy else-branch; only the unified-path keys remain |
</interfaces>

<reference_decisions>
Locked decisions from 34-CONTEXT.md:
- **D-17:** Plan 3 ships as 5 commits (A-E) in the dependency order above. Writes stay dual via Plan 2 shim THROUGHOUT. Plan 4 removes the shim, not Plan 3.
- **ARCHITECTURE.md Irreducible Coupling #2:** "Template read swap + query allowlist swap — Plan 3 — Templates sort-by-haiku_score URL parameter 500s if query allowlist drops haiku_score first." Commit A (query) MUST land before Commit D (templates).

Claude's Discretion:
- SQL implementation of classification_rank: CASE expression inline in ORDER BY vs a view vs a Python-computed column — pick inline CASE for simplicity
- Tiebreak between same-classification jobs: sum of sub_scores (6 values 1-5, range 6-30) — or any deterministic per-row secondary sort; pick sum for simplicity
- Template sub-score display widget: horizontal bars, radar chart, or compact numeric grid — pick horizontal bars with color-coded fill (no new deps; Tailwind CDN utility classes)
- `allowed_sort_cols` may keep legacy keys 'haiku_score', 'sonnet_score' as aliases that translate to sub_score_sum + classification_rank for bookmark back-compat; Plan 4 removes the aliases
</reference_decisions>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Commit A — query-layer migration (db.py, exclusion_filter, careers_crawler, ats_scanner, agentic_enricher, companies blueprint, dedup_normalizer, rejection_analyzer, rejection_patterns)</name>
  <files>job_finder/db.py, job_finder/web/exclusion_filter.py, job_finder/web/careers_crawler.py, job_finder/web/ats_scanner.py, job_finder/web/agentic_enricher.py, job_finder/web/blueprints/companies.py, job_finder/web/dedup_normalizer.py, job_finder/web/rejection_analyzer.py, job_finder/web/rejection_patterns.py, tests/test_careers_crawler.py, tests/test_ats_scanner.py, tests/test_rejection_patterns.py, tests/test_rejection_analyzer.py, tests/test_dedup_normalizer.py, tests/test_agentic_enricher.py</files>

  <read_first>
    - Each of the 9 production files listed in `<files>` — read the relevant line ranges per the interfaces table above (do NOT read entire files where only ~20 lines need to change)
    - `rtk grep -n "haiku_score\\|sonnet_score" job_finder/db.py job_finder/web/exclusion_filter.py job_finder/web/careers_crawler.py` — confirm exact current shape before editing
    - tests/test_* for each modified production file — understand current fixtures and assertion patterns
    - .planning/research/ARCHITECTURE.md lines 120-141 (Data flow junction inventory — authoritative file:line list)
    - .planning/phases/34-greenfield-scorer-rewrite/34-CONTEXT.md §D-17 commit A bullets
  </read_first>

  <behavior>
    - Test 1 (db.get_filtered_jobs classification filter): filter={"classification": ["apply","consider"]} returns rows where classification IN those values.
    - Test 2 (db.get_filtered_jobs ordering): With default sort, ORDER BY uses classification_rank CASE (apply=4, consider=3, skip=2, reject=1) DESC + sub_score_sum DESC. Two apply-rows ordered by sub_score_sum.
    - Test 3 (db.get_filtered_jobs legacy sort-by alias): sort_by='haiku_score' (URL back-compat) translates internally to classification_rank + sub_score_sum and returns rows in that order. No SQL error.
    - Test 4 (exclusion_filter.count_scorable): Returns count of jobs with classification IS NULL AND jd_full IS NOT NULL. Old function name count_haiku_scorable either removed or shim-forwarded to count_scorable (pick one; plan 4 removes any shim).
    - Test 5 (careers_crawler gate): The query at line 625 now uses `WHERE j.classification IN ('apply','consider')` — grep confirms. Test uses fixture jobs with different classifications and verifies only apply+consider rows returned.
    - Test 6 (ats_scanner summary counters): `summary["scored"]` increments on each classified row; `summary["classified_apply"]` etc. track per-classification counts. `summary["haiku_scored"]` NOT in the new block.
    - Test 7 (agentic_enricher ORDER BY): query at line 500 uses classification_rank CASE; highest-priority rows come first.
    - Test 8 (blueprints/companies.py): effective_score subquery uses classification-rank ordering; companies page still renders.
    - Test 9 (dedup_normalizer merge): When merging two rows — left.classification='apply', right.classification='consider' — merged row has classification='apply' (priority-max). sub_scores merged element-wise max: merged[k] = max(left[k], right[k]).
    - Test 10 (rejection_analyzer): SELECT column list now includes classification, sub_scores_json, fit_analysis (NOT haiku_score / sonnet_score). Prompt template updated to reference classification + sub-scores.
    - Test 11 (rejection_patterns.RejectionPattern): Dataclass fields renamed from haiku_score/sonnet_score to classification/sub_scores. Backward-compat __init__ with old kwargs is OPTIONAL — prefer clean rename; update all constructors.
    - Test 12 (no production reader left reading haiku_score/sonnet_score): `rtk grep -rn "haiku_score\\b\\|sonnet_score\\b" job_finder/web/ --include='*.py'` shows zero READ sites in production code after Commit A lands (WRITE sites via Plan 2 shim are OK; they're in scoring_orchestrator.py which Commit A doesn't touch).
  </behavior>

  <action>
    Step 1 — `job_finder/db.py` (`get_filtered_jobs` function around lines 472-492 and 540-545):
    - Replace the COALESCE score ordering with a classification_rank CASE. Example SQL fragment:
      ```sql
      ORDER BY
          CASE classification
              WHEN 'apply'    THEN 4
              WHEN 'consider' THEN 3
              WHEN 'skip'     THEN 2
              WHEN 'reject'   THEN 1
              ELSE 0
          END DESC,
          (COALESCE(json_extract(sub_scores_json, '$.title_fit'), 0) +
           COALESCE(json_extract(sub_scores_json, '$.location_fit'), 0) +
           COALESCE(json_extract(sub_scores_json, '$.comp_fit'), 0) +
           COALESCE(json_extract(sub_scores_json, '$.domain_match'), 0) +
           COALESCE(json_extract(sub_scores_json, '$.seniority_match'), 0) +
           COALESCE(json_extract(sub_scores_json, '$.skills_match'), 0)) DESC
      ```
    - Replace min_score/max_score filter predicates with `classification IN (...)` when the caller passes a list of classifications. If callers still pass min_score/max_score, translate with a documented mapping (e.g., map >=80 to apply-only, >=60 to apply+consider, >=40 to apply+consider+skip).
    - `allowed_sort_cols`: drop `haiku_score`, `sonnet_score`; add `classification_rank`, `sub_score_sum`. Keep `haiku_score` and `sonnet_score` in a SEPARATE `_LEGACY_SORT_ALIAS` dict that translates to classification_rank — this lets old bookmarks work; Plan 4 removes.

    Step 2 — `job_finder/web/exclusion_filter.py` (line 90):
    - Rename `count_haiku_scorable` to `count_scorable`.
    - Predicate: `WHERE classification IS NULL AND jd_full IS NOT NULL`.
    - Update all callers (grep for `count_haiku_scorable` in job_finder/ and tests/ and rename). If callers are cross-plan sensitive, add a one-line shim `count_haiku_scorable = count_scorable` at module bottom with a `# PLAN-4-REMOVE` comment.

    Step 3 — `job_finder/web/careers_crawler.py`:
    - Line 625: replace `WHERE j.haiku_score >= ?` with `WHERE j.classification IN ('apply','consider')`. Drop the threshold parameter from the call chain.
    - Lines 559-703, 1258: replace `summary["haiku_scored"]` += … and `summary["sonnet_evaluated"]` += … with `summary["scored"]` and per-classification counters matching Plan 2 Task 1 run_scoring summary keys.

    Step 4 — `job_finder/web/ats_scanner.py` lines 675-1100: same counter-collapse as careers_crawler.

    Step 5 — `job_finder/web/agentic_enricher.py` line 500: replace ORDER BY with classification_rank CASE + sub_score_sum.

    Step 6 — `job_finder/web/blueprints/companies.py` lines 141, 144, 292-293: replace effective_score subquery with classification-rank ordering.

    Step 7 — `job_finder/web/dedup_normalizer.py` lines 287-394:
    - UPDATE/merge logic: classification merged by max-priority (apply > consider > skip > reject); sub_scores merged element-wise max per key.
    - Reference implementation:
      ```python
      _CLASSIFICATION_RANK = {"apply": 4, "consider": 3, "skip": 2, "reject": 1, None: 0, "": 0}

      def _merge_classification(a: str | None, b: str | None) -> str | None:
          if _CLASSIFICATION_RANK.get(a, 0) >= _CLASSIFICATION_RANK.get(b, 0):
              return a or b
          return b

      def _merge_sub_scores(a: dict | None, b: dict | None) -> dict:
          a = a or {}
          b = b or {}
          keys = set(a) | set(b)
          return {k: max(a.get(k, 0), b.get(k, 0)) for k in keys}
      ```

    Step 8 — `job_finder/web/rejection_analyzer.py` lines 134-173: update SELECT column list to include classification, sub_scores_json, fit_analysis (DROP haiku_score, sonnet_score from select). Update the prompt text that references the fields.

    Step 9 — `job_finder/web/rejection_patterns.py` lines 24-200:
    - RejectionPattern dataclass: rename `haiku_score` → `classification` (str), `sonnet_score` → `sub_scores` (dict).
    - All call sites and test fixtures updated to the new field names.

    Step 10 — Update tests in the `<files>` list. Most are targeted rename or assertion updates — `haiku_score >= 60` → `classification IN ('apply','consider')`. For rejection_patterns tests, update fixture construction to pass new kwargs.

    Step 11 — Verify no read-site leaks:
    ```bash
    rtk grep -rn '"haiku_score"\\|"sonnet_score"' job_finder/web/ --include='*.py' --exclude='scoring_orchestrator.py' --exclude='scoring_runner.py' --exclude='haiku_scorer.py' --exclude='sonnet_evaluator.py' --exclude='db_migrate.py'
    ```
    Expect zero or near-zero hits (only the legacy-shim WRITE site in scoring_orchestrator.py and the legacy module files Plan 4 deletes).

    Step 12 — Run `uv run --active pytest -q --tb=short`. Must exit 0. Commit as `feat(34-03-A): migrate query-layer consumers to classification + sub_scores_json`.
  </action>

  <verify>
    <automated>uv run --active pytest -q --tb=short</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "j\\.classification IN" job_finder/web/careers_crawler.py` returns a match around line 625.
    - `grep -n "def count_scorable" job_finder/web/exclusion_filter.py` returns a match.
    - `grep -n "classification_rank\\|CASE classification" job_finder/db.py` returns matches inside get_filtered_jobs.
    - `grep -n "classification_rank\\|CASE classification" job_finder/web/agentic_enricher.py` shows the new ORDER BY.
    - `grep -n "_merge_classification\\|_CLASSIFICATION_RANK" job_finder/web/dedup_normalizer.py` returns matches.
    - `grep -n "classification\\|sub_scores" job_finder/web/rejection_patterns.py` shows the renamed dataclass fields.
    - `rtk grep -rn '"haiku_score"' job_finder/web/ --include='*.py' --exclude='scoring_orchestrator.py' --exclude='scoring_runner.py' --exclude='haiku_scorer.py' --exclude='sonnet_evaluator.py' --exclude='db_migrate.py'` shows zero results.
    - `uv run --active pytest -q --tb=short` exits 0.
    - Git log shows exactly one commit `feat(34-03-A): ...` covering the files in this task.
  </acceptance_criteria>

  <done>
    All query-layer readers migrated to classification + sub_scores_json. Dedup-merge logic handles classification priority + element-wise sub_score max. Rejection analyzer/patterns use new columns/fields. Plan 2 shim still populates legacy columns → safe revert. Full suite green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Commit B — batch_scoring.py route + background-job merge (haiku + sonnet → single scoring)</name>
  <files>job_finder/web/blueprints/batch_scoring.py, tests/test_batch_scoring.py</files>

  <read_first>
    - job_finder/web/blueprints/batch_scoring.py entire file (lines 21-442 per ARCHITECTURE.md; understand session_type, route handlers, background job launcher pattern, progress tracking)
    - tests/test_batch_scoring.py entire file (route handlers tested via Flask test client; understand fixture patterns)
    - Commit A output — verify query-layer uses classification correctly (predicates in batch_scoring depend on Commit A's db.py changes)
  </read_first>

  <behavior>
    - Test 1 (single scoring route): POST /batch-score/start returns 200 with session_id; GET /batch-score/status/<id> returns scoring progress. Old haiku-specific and sonnet-specific routes redirect to /batch-score/start OR return 404 — decide and document (recommend: they return 404 to force migration; templates already updated in Plan 3 Commit C/D).
    - Test 2 (_run_batch_bg): Background worker pulls dedup_keys WHERE classification IS NULL, invokes score_and_persist_job (unified path), updates session progress.
    - Test 3 (session_type enum): Only "scoring" and "sync" accepted. Old "haiku"/"sonnet" values rejected.
    - Test 4 (label "Scoring"): Progress JSON returns label="Scoring" (not "Haiku" or "Sonnet").
    - Test 5 (query predicate): WHERE clause uses classification IN (…) / classification IS NULL — no haiku_score predicates remain.
  </behavior>

  <action>
    Step 1 — `job_finder/web/blueprints/batch_scoring.py`:
    - MERGE `batch_score_haiku_start` (lines 21-72) + `batch_score_sonnet_start` (lines 74-129) into `batch_score_start`. New URL: `/batch-score/start` (or similar — match project HTMX convention).
    - MERGE `_run_batch_haiku_bg` + `_run_batch_sonnet_bg` into `_run_batch_bg`. Internally calls `score_and_persist_job` from scoring_orchestrator.
    - Update session_type enum: replace instances of "haiku"/"sonnet" with "scoring". Keep "sync".
    - Replace "Haiku" / "Sonnet" label strings (lines 157, 249 per ARCHITECTURE.md) with "Scoring".
    - Replace predicates `haiku_score IS NOT NULL AND haiku_score >= ?` (lines 88, 287, 386-387) with `classification IN (...)`.
    - Delete old haiku-specific and sonnet-specific route handlers (no longer registered).

    Step 2 — `tests/test_batch_scoring.py`:
    - Update test class to test the single unified route instead of two separate routes. Merge assertions from prior haiku + sonnet tests.
    - Any template that posts to /batch-score/haiku or /batch-score/sonnet URL needs to post to /batch-score/start. Update template files in Commit D (separately — don't pre-touch).

    Step 3 — Quick manual inspection of templates that reference the old URLs. These won't be edited in Commit B but note them for Commit D:
    ```bash
    rtk grep -rn "batch-score/haiku\\|batch-score/sonnet" job_finder/web/templates/
    ```

    Step 4 — `uv run --active pytest tests/test_batch_scoring.py -q --tb=short` and full suite `uv run --active pytest -q --tb=short`. Exit 0.

    Step 5 — Commit as `feat(34-03-B): merge batch_score_haiku/sonnet routes into unified batch_score_start`.
  </action>

  <verify>
    <automated>uv run --active pytest tests/test_batch_scoring.py -q --tb=short && uv run --active pytest -q --tb=short</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "def batch_score_start\\|def _run_batch_bg" job_finder/web/blueprints/batch_scoring.py` returns exactly two matches.
    - `grep -n "def batch_score_haiku_start\\|def batch_score_sonnet_start\\|def _run_batch_haiku_bg\\|def _run_batch_sonnet_bg" job_finder/web/blueprints/batch_scoring.py` returns zero matches.
    - `grep -n "session_type\\s*==\\s*['\"]haiku['\"]\\|session_type\\s*==\\s*['\"]sonnet['\"]" job_finder/web/blueprints/batch_scoring.py` returns zero matches.
    - `grep -n "haiku_score\\s*IS NOT NULL\\|haiku_score\\s*>=" job_finder/web/blueprints/batch_scoring.py` returns zero matches.
    - `grep -n "classification IN\\|classification IS NULL" job_finder/web/blueprints/batch_scoring.py` returns at least one match.
    - `uv run --active pytest -q --tb=short` exits 0.
    - Git log shows `feat(34-03-B): ...` commit.
  </acceptance_criteria>

  <done>
    Batch scoring has one route + one background worker. session_type enum collapsed. All predicates use classification. Tests merged to cover the unified path. Suite green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Commit C — dashboard.py quick-actions + stats merge (scoring_eligible_count, _cached_tier_available('scoring'))</name>
  <files>job_finder/web/blueprints/dashboard.py, tests/test_views.py</files>

  <read_first>
    - job_finder/web/blueprints/dashboard.py lines 180-194 (Haiku/Sonnet eligible counts block)
    - tests/test_views.py (tests for dashboard/stats endpoints)
    - `rtk grep -n "_cached_tier_available" job_finder/web/blueprints/dashboard.py` — understand how the tier-availability cache is consumed
  </read_first>

  <behavior>
    - Test 1 (scoring_eligible_count replaces separate counts): Dashboard stats endpoint returns `scoring_eligible_count` with SUM of jobs WHERE classification IS NULL AND jd_full IS NOT NULL. The keys `haiku_eligible_count`, `sonnet_eligible_count` NOT in response.
    - Test 2 (_cached_tier_available scoring): `_cached_tier_available("scoring")` returns True when providers.scoring has a valid model + provider configured. Old calls with "haiku"/"sonnet" still return booleans (cache shim — Plan 4 removes).
    - Test 3 (dashboard page renders): GET / returns 200 with unified single-row scoring stat (not two rows).
  </behavior>

  <action>
    Step 1 — `job_finder/web/blueprints/dashboard.py` lines 180-194:
    - Remove `haiku_eligible_count` and `sonnet_eligible_count` queries.
    - Replace with single query: `SELECT COUNT(*) FROM jobs WHERE classification IS NULL AND jd_full IS NOT NULL` → `scoring_eligible_count`.
    - Replace `_cached_tier_available("haiku")` / `_cached_tier_available("sonnet")` with `_cached_tier_available("scoring")`.
    - Update any stats context dict keys to use `scoring_eligible_count` (template changes land in Commit D).

    Step 2 — `tests/test_views.py`: update stats endpoint tests to assert scoring_eligible_count key + remove haiku/sonnet assertions.

    Step 3 — `uv run --active pytest tests/test_views.py -q --tb=short` + full `uv run --active pytest -q --tb=short`. Exit 0.

    Step 4 — Commit as `feat(34-03-C): unify dashboard stats to scoring_eligible_count`.
  </action>

  <verify>
    <automated>uv run --active pytest tests/test_views.py -q --tb=short && uv run --active pytest -q --tb=short</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "scoring_eligible_count" job_finder/web/blueprints/dashboard.py` returns a match.
    - `grep -n "haiku_eligible_count\\|sonnet_eligible_count" job_finder/web/blueprints/dashboard.py` returns zero matches.
    - `grep -n "_cached_tier_available(\"scoring\")" job_finder/web/blueprints/dashboard.py` returns a match.
    - `uv run --active pytest -q --tb=short` exits 0.
    - Git log shows `feat(34-03-C): ...` commit.
  </acceptance_criteria>

  <done>
    Dashboard uses unified scoring_eligible_count. Tier-available cache updated. Templates for dashboard still render (full layout changes in Commit D). Suite green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Commit D — template updates (_row_detail, _row_expanded, _score_cell, _resume_section, costs/index) swap numeric-score → classification badge + sub-score display</name>
  <files>job_finder/web/templates/jobs/_row_detail.html, job_finder/web/templates/jobs/_row_expanded.html, job_finder/web/templates/jobs/_score_cell.html, job_finder/web/templates/jobs/_resume_section.html, job_finder/web/templates/costs/index.html, tests/test_views.py</files>

  <read_first>
    - job_finder/web/templates/jobs/_row_detail.html (lines 6, 67, 109-149 per ARCHITECTURE.md — understand current numeric-score rendering + any HTMX wiring)
    - job_finder/web/templates/jobs/_row_expanded.html (lines 45, 104-213)
    - job_finder/web/templates/jobs/_score_cell.html (line 9)
    - job_finder/web/templates/jobs/_resume_section.html (lines 54, 80)
    - job_finder/web/templates/costs/index.html (line 148)
    - CLAUDE.md HTMX patterns section (fragment routes return full page when HX-Request absent; hx-target/hx-swap correctness; 200 not 204)
    - Existing `from_json` Jinja filter registration (already present in project per CLAUDE.md conventions list)
  </read_first>

  <behavior>
    - Test 1 (_score_cell.html renders classification badge): For a job with classification='apply', rendering _score_cell contains text "apply" and a CSS class signaling green (e.g., bg-green-100 or badge-apply).
    - Test 2 (_row_detail.html sub-scores): Row detail renders all 6 sub-score dimensions (from job.sub_scores_json|from_json) with labels (Title fit, Location fit, etc.) and values 1-5.
    - Test 3 (_row_expanded.html rationale): Expanded row displays rationale strengths, gaps, talking_points, resume_priority_skills as bulleted lists from fit_analysis|from_json.
    - Test 4 (no template references legacy fields): grep on all 5 templates shows ZERO references to job.haiku_score, job.sonnet_score, job.haiku_summary.
    - Test 5 (costs/index.html legend): Line 148 area shows a "Scoring" legend entry (not "Haiku" or "Sonnet" separately).
    - Test 6 (_resume_section.html gating): Reference to classification (e.g., "if job.classification == 'apply'" or equivalent template-level conditional) at lines 54, 80.
  </behavior>

  <action>
    Step 1 — `_score_cell.html` (line 9 and surroundings): replace the numeric score rendering with a classification badge. Use Tailwind utility classes for color-coding:
    ```jinja
    {% set cls = job.classification %}
    {% if cls == 'apply' %}
      <span class="inline-block px-2 py-1 text-xs font-semibold rounded bg-green-100 text-green-800">Apply</span>
    {% elif cls == 'consider' %}
      <span class="inline-block px-2 py-1 text-xs font-semibold rounded bg-amber-100 text-amber-800">Consider</span>
    {% elif cls == 'skip' %}
      <span class="inline-block px-2 py-1 text-xs font-semibold rounded bg-gray-100 text-gray-700">Skip</span>
    {% elif cls == 'reject' %}
      <span class="inline-block px-2 py-1 text-xs font-semibold rounded bg-red-100 text-red-800">Reject</span>
    {% else %}
      <span class="inline-block px-2 py-1 text-xs font-semibold rounded bg-slate-50 text-slate-500">Unscored</span>
    {% endif %}
    ```

    Step 2 — `_row_detail.html` (lines 6, 67, 109-149): Replace the numeric score block with:
    - The same classification badge (reuse _score_cell.html via {% include %} if it renders the full badge; otherwise inline the same markup).
    - A 6-row sub-score display. Recommended markup (horizontal bars) — values 1-5 as a width percentage (20%, 40%, 60%, 80%, 100%):
    ```jinja
    {% set ss = job.sub_scores_json|from_json if job.sub_scores_json else {} %}
    {% set dim_labels = [
      ('title_fit', 'Title fit'),
      ('location_fit', 'Location fit'),
      ('comp_fit', 'Compensation'),
      ('domain_match', 'Domain'),
      ('seniority_match', 'Seniority'),
      ('skills_match', 'Skills'),
    ] %}
    <div class="sub-scores space-y-1">
      {% for key, label in dim_labels %}
        {% set v = ss.get(key, 0) %}
        <div class="flex items-center gap-2">
          <span class="w-24 text-xs text-slate-600">{{ label }}</span>
          <div class="flex-1 bg-slate-100 h-2 rounded">
            <div class="h-2 rounded bg-slate-500" style="width: {{ (v * 20) }}%"></div>
          </div>
          <span class="w-6 text-xs text-slate-700">{{ v }}</span>
        </div>
      {% endfor %}
    </div>
    ```
    Remove all references to job.haiku_score, job.sonnet_score, job.haiku_summary from this template.

    Step 3 — `_row_expanded.html` (lines 45, 104-213): Replace score-numeric blocks with the classification badge + sub-score display (same pattern as _row_detail). Rationale display: use `{% set fa = job.fit_analysis|from_json if job.fit_analysis else {} %}` and render strengths, gaps, talking_points, resume_priority_skills each as `<ul>` of `<li>` items.

    Step 4 — `_resume_section.html` (lines 54, 80): Replace `{% if job.sonnet_score %}` / numeric-threshold blocks with `{% if job.classification == 'apply' %}` (or `in ['apply']` — single-valued comparison suffices).

    Step 5 — `costs/index.html` (line 148): Replace "Haiku" + "Sonnet" legend entries with "Scoring" legend entry. Match the existing legend styling.

    Step 6 — Update any template that linked to `/batch-score/haiku` or `/batch-score/sonnet` (found in Commit B's inspection) to link to `/batch-score/start`.

    Step 7 — `tests/test_views.py`: update HTMX response body assertions to check for classification-badge text (Apply/Consider/Skip/Reject) and sub-score markup, instead of numeric-score strings.

    Step 8 — Run the app locally and manually verify (if feasible): visit the jobs list page, confirm score cells render badges. HTMX expand a row, confirm sub-scores and rationale show. Visit /costs, confirm single Scoring legend. Auto-verification:
    ```bash
    uv run --active pytest tests/test_views.py -q --tb=short
    uv run --active pytest -q --tb=short
    ```
    Exits 0.

    Step 9 — Commit as `feat(34-03-D): templates render classification badge + sub-score breakdown`.
  </action>

  <verify>
    <automated>uv run --active pytest tests/test_views.py -q --tb=short && uv run --active pytest -q --tb=short</automated>
  </verify>

  <acceptance_criteria>
    - `grep -rn "haiku_score\\|sonnet_score\\|haiku_summary" job_finder/web/templates/` returns zero matches.
    - `grep -rn "job\\.classification" job_finder/web/templates/jobs/` returns matches in all 4 jobs/ templates.
    - `grep -rn "sub_scores_json\\|from_json" job_finder/web/templates/jobs/_row_detail.html` returns matches.
    - `grep -n "Scoring" job_finder/web/templates/costs/index.html` shows the new legend entry.
    - `grep -rn "batch-score/haiku\\|batch-score/sonnet" job_finder/web/templates/` returns zero matches.
    - `uv run --active pytest -q --tb=short` exits 0.
    - Git log shows `feat(34-03-D): ...` commit.
  </acceptance_criteria>

  <done>
    All 5 templates render classification badges + sub-score breakdowns. Template tests pass. No legacy column references in any template. Suite green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Commit E — resume_generator gating flip + pipeline_runner.run_ingestion legacy-else removal (summary key collapse)</name>
  <files>job_finder/web/resume_generator.py, job_finder/web/pipeline_runner.py, tests/test_resume.py, tests/test_ingestion.py</files>

  <read_first>
    - job_finder/web/resume_generator.py lines 415-430 (multi_threshold gate)
    - job_finder/web/pipeline_runner.py run_ingestion (Plan 2 Task 2 output — the flag-gated branch with legacy else-branch)
    - tests/test_resume.py tests around the multi-version gate
    - tests/test_ingestion.py summary-key assertions
  </read_first>

  <behavior>
    - Test 1 (resume gating flip): Passing a job with classification='apply' returns True from the multi-version precondition; classification in ['consider','skip','reject',None] returns False. Legacy sonnet_score comparison gone.
    - Test 2 (pipeline_runner legacy else-branch removed): run_ingestion no longer branches on use_unified_scorer — always calls run_scoring. The `use_unified_scorer` key can stay in config but is effectively ignored (Plan 4 removes the key). Summary has ONLY scored + classified_* keys — haiku_scored / sonnet_queued / sonnet_evaluated NOT in summary.
    - Test 3 (old tests updated): Any test asserting `summary['haiku_scored']` now asserts `summary['scored']` or `summary['classified_*']`.
  </behavior>

  <action>
    Step 1 — `job_finder/web/resume_generator.py` lines 415-430:
    - Replace `if job.get("sonnet_score") >= multi_threshold:` (or similar) with `if job.get("classification") == "apply":`.
    - Remove any `multi_threshold` config lookup if it's no longer used anywhere (grep to confirm). If it's used elsewhere, leave in place but unused for this gate.

    Step 2 — `job_finder/web/pipeline_runner.py`:
    - In run_ingestion, DELETE the `else:` branch (the legacy Haiku→Sonnet two-phase block). Keep only the unified path.
    - Collapse summary keys: remove any residual `summary["haiku_scored"]`, `summary["sonnet_queued"]`, `summary["sonnet_evaluated"]` that were populated by the legacy path. Unified path already uses `scored` + `classified_*`.
    - Keep the `if config.get("use_unified_scorer", False):` guard temporarily; Plan 4 removes the flag entirely. OR remove the guard now and always run unified — document choice in commit body.
    - Recommended: remove the guard now since Plan 2 Commit B already flipped the flag to True and smoke-tested. Any legacy-path callers that still exist (should be zero) will hit the unified path too.

    Step 3 — Update tests: `tests/test_resume.py` gate tests assert classification. `tests/test_ingestion.py` summary-key tests assert unified keys.

    Step 4 — `uv run --active pytest -q --tb=short` exits 0.

    Step 5 — Commit as `feat(34-03-E): resume gating + pipeline summary keys collapse to classification`.
  </action>

  <verify>
    <automated>uv run --active pytest tests/test_resume.py tests/test_ingestion.py -q --tb=short && uv run --active pytest -q --tb=short</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "sonnet_score.*multi_threshold\\|sonnet_score\\s*>=" job_finder/web/resume_generator.py` returns zero matches.
    - `grep -n "classification\\s*==\\s*['\"]apply['\"]" job_finder/web/resume_generator.py` returns a match.
    - `grep -n "haiku_scored\\|sonnet_queued\\|sonnet_evaluated" job_finder/web/pipeline_runner.py` returns zero matches.
    - `grep -n "scored\\|classified_apply" job_finder/web/pipeline_runner.py` returns matches (summary keys).
    - `uv run --active pytest -q --tb=short` exits 0.
    - Git log shows `feat(34-03-E): ...` commit — Plan 3 complete with 5 commits A/B/C/D/E.
  </acceptance_criteria>

  <done>
    Resume multi-version gate uses classification. Pipeline summary keys collapsed. Plan 3 complete with all 5 revertable commits. All reads now use new columns; writes still dual via Plan 2 shim (Plan 4 removes). Suite green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL sort-by param → SQL | Python allowlist check gates sort column; legacy aliases translated internally to classification_rank (no raw user-supplied column names interpolated) |
| Template Jinja2 → fit_analysis JSON | User-controlled JSON deserialization via from_json filter; KeyError on missing keys handled with `|default` patterns |
| Batch-scoring route handler → worker | session_id state in SQLite; unique IDs; worker polling pattern unchanged from Phase 2 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-09 | Tampering | URL ?sort=haiku_score bookmark 500s after allowlist change | mitigate | Keep `haiku_score` and `sonnet_score` in a _LEGACY_SORT_ALIAS dict that translates to classification_rank in Commit A; Plan 4 removes aliases (by then all bookmarks regenerated) |
| T-34-10 | Information-disclosure | Templates render untrusted rationale strings without escaping | mitigate | Jinja2 auto-escapes by default; strings from fit_analysis rendered via `{{ text }}` not `{{ text|safe }}` — verify no |safe filter added during template migration |
| T-34-11 | Denial-of-service | json_extract on NULL sub_scores_json fails SQL | mitigate | COALESCE(json_extract(…), 0) wraps every sub-score reference in ORDER BY (Task 1) so NULL fit_analysis doesn't crash the sort |
| T-34-12 | Tampering | Commit A lands query changes but not B (batch-scoring); batch-scoring still posts haiku_score predicates to the migrated query layer | accept | Commit order A→B→C→D→E chosen so the intermediate state is internally consistent; each commit fully green on its own |
</threat_model>

<verification>
After Plan 3 commits A-E:

1. `uv run --active pytest -q --tb=short` exits 0 after EACH commit individually (verify via git bisect or sequential checkout if needed).
2. `rtk grep -rn '"haiku_score"\\|"sonnet_score"\\|"haiku_summary"' job_finder/web/ --include='*.py' --exclude='scoring_orchestrator.py' --exclude='scoring_runner.py' --exclude='haiku_scorer.py' --exclude='sonnet_evaluator.py' --exclude='db_migrate.py'` returns zero matches — all readers migrated.
3. `rtk grep -rn "haiku_score\\|sonnet_score\\|haiku_summary" job_finder/web/templates/` returns zero matches.
4. Live app renders correctly: dashboard page loads, jobs list shows classification badges, row expansion shows sub-scores + rationale lists, batch-scoring page posts to /batch-score/start, costs page shows single Scoring legend.
5. `rtk git log --oneline -5` shows Plan 3's five commits in order: `feat(34-03-A): ...`, `feat(34-03-B): ...`, `feat(34-03-C): ...`, `feat(34-03-D): ...`, `feat(34-03-E): ...`.
6. The Plan 2 legacy-shim write is still in place in scoring_orchestrator.py — verified via `grep -n "haiku_score\\s*=" job_finder/web/scoring_orchestrator.py` (Plan 4 removes).
</verification>

<success_criteria>
- All 16 CONSUMERS-* requirements addressed across the 5 commits.
- All 8 TESTS-* requirements (08, 09, 10, 11, 12, 13, 14, 19) addressed via test file updates.
- Every individual commit (A-E) leaves the test suite green — no temporarily-broken states.
- System functional end-to-end between Plan 3 and Plan 4 (legacy-shim writes active; all reads use new columns).
- No production read site references `haiku_score`, `sonnet_score`, or `haiku_summary` after Plan 3.
</success_criteria>

<output>
After completion, create `.planning/phases/34-greenfield-scorer-rewrite/34-03-SUMMARY.md` documenting the 5-commit sequence, per-commit file lists, and any legacy-sort-alias table added for URL back-compat.
</output>
