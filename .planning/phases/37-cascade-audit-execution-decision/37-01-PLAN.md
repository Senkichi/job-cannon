---
phase: 37
plan: 01
type: execute
wave: 1
depends_on:
  - 36
files_modified:
  - evals/cascade_audit/run_audit.py
  - CASCADE-AUDIT.md
  - tests/test_audit_execution.py
autonomous: true
requirements:
  - AUDIT-09
  - AUDIT-10
  - AUDIT-11
  - AUDIT-12
---

# Plan 01: Execute Cascade Audit and Generate Decision Report

## Objective

Execute the 3-round shadow-replay audit (R0 calibration / R1 contenders / R2 head-to-head) using the harness from Phase 36, generate CASCADE-AUDIT.md with per-callsite verdicts and Case A/B decision, and verify the decision is explicit for Phase 40's config rewire.

## Tasks

### Task 01-01: Create Audit CLI Entrypoint

<read_first>
- evals/cascade_audit/harness.py (Phase 36 harness orchestration pattern)
- evals/cascade_audit/run_audit.py (target file to create)
</read_first>

<action>
Create evals/cascade_audit/run_audit.py with argparse CLI that orchestrates the 3-round audit:

1. Import argparse and AuditHarness from evals.cascade_audit.harness
2. Create main() function with argparse parser:
   - --round argument with choices ['r0', 'r1', 'r2'] (required)
   - --callsite argument for specific callsite or None for all (optional)
3. Instantiate AuditHarness with db_path='jobs.db'
4. Route to appropriate harness method based on --round:
   - 'r0' → harness.run_calibration(callsite=args.callsite)
   - 'r1' → harness.run_contenders(callsite=args.callsite)
   - 'r2' → harness.run_head_to_head(callsite=args.callsite)
5. Add if __name__ == '__main__': main() guard
6. Add shebang: #!/usr/bin/env python3
</action>

<acceptance_criteria>
- evals/cascade_audit/run_audit.py exists
- run_audit.py contains import argparse
- run_audit.py contains from evals.cascade_audit.harness import AuditHarness
- run_audit.py contains parser.add_argument('--round', choices=['r0', 'r1', 'r2'], required=True)
- run_audit.py contains parser.add_argument('--callsite')
- run_audit.py contains if args.round == 'r0': harness.run_calibration
- run_audit.py contains if args.round == 'r1': harness.run_contenders
- run_audit.py contains if args.round == 'r2': harness.run_head_to_head
- run_audit.py contains if __name__ == '__main__': main()
</acceptance_criteria>

### Task 01-02: Add Resume Schedulers Prompt to Harness

<read_first>
- evals/cascade_audit/harness.py (target file to modify)
- .planning/phases/37-cascade-audit-execution-decision/37-CONTEXT.md (decision D-06)
</read_first>

<action>
Modify evals/cascade_audit/harness.py to emit explicit "RESUME SCHEDULERS" prompt at end of Round 2:

1. Locate the run_head_to_head method in harness.py
2. After all R2 artifacts are written and before method return, add:
   ```python
   print("\n" + "="*60)
   print("RESUME SCHEDULERS")
   print("="*60)
   print("\nAudit Round 2 (R2) complete.")
   print("Re-enable APScheduler jobs to resume background ingest/scoring:")
   print("  1. Open Flask app at http://localhost:5000/settings/scheduler")
   print("  2. Click 'Enable Scheduler' if disabled")
   print("  3. Verify jobs are running in scheduler status")
   print("\n" + "="*60 + "\n")
   ```
3. Ensure prompt is unmissable (uppercase, border, newline padding)
</action>

<acceptance_criteria>
- harness.py contains print("RESUME SCHEDULERS")
- harness.py contains print("Re-enable APScheduler jobs")
- harness.py contains print("http://localhost:5000/settings/scheduler")
- The prompt appears after R2 artifact writes in run_head_to_head method
</acceptance_criteria>

### Task 01-03: Execute R0 Calibration Round

<read_first>
- evals/cascade_audit/harness.py (harness run_calibration method)
- evals/cascade_audit/run_audit.py (CLI entrypoint)
</read_first>

<action>
Execute R0 calibration round via CLI:

1. Run: uv run python -m evals.cascade_audit.run_audit --round r0
2. Verify harness completes without errors
3. Verify artifacts created in evals/cascade_audit/artifacts/round_0/
4. Verify each of 6 callsites has artifact: parse_structured_fields_r0.json, find_careers_url_r0.json, extract_jobs_r0.json, description_reformat_r0.json, company_research_r0.json, ai_nav_discovery_r0.json
5. Verify each artifact contains environment provenance block (config_snapshot, model_versions, commit_sha)
6. If any catastrophic failure occurs (harness bugs, provider outages), halt audit per D-01 fail-fast
</action>

<acceptance_criteria>
- Command uv run python -m evals.cascade_audit.run_audit --round r0 exits 0
- Directory evals/cascade_audit/artifacts/round_0/ exists
- File parse_structured_fields_r0.json exists in round_0/
- File find_careers_url_r0.json exists in round_0/
- File extract_jobs_r0.json exists in round_0/
- File description_reformat_r0.json exists in round_0/
- File company_research_r0.json exists in round_0/
- File ai_nav_discovery_r0.json exists in round_0/
- Each artifact contains "config_snapshot" key
- Each artifact contains "model_versions" key
- Each artifact contains "commit_sha" key
</acceptance_criteria>

### Task 01-04: Execute R1 Contenders Round

<read_first>
- evals/cascade_audit/harness.py (harness run_contenders method)
- evals/cascade_audit/run_audit.py (CLI entrypoint)
</read_first>

<action>
Execute R1 contenders round via CLI:

1. Run: uv run python -m evals.cascade_audit.run_audit --round r1
2. Verify harness completes without errors
3. Verify artifacts created in evals/cascade_audit/artifacts/round_1/
4. Verify each callsite has artifact with n=10 sample size per spec
5. Verify artifacts contain gate measurements (accuracy, latency, cost, schema_valid)
6. If any catastrophic failure occurs, halt audit per D-01 fail-fast
</action>

<acceptance_criteria>
- Command uv run python -m evals.cascade_audit.run_audit --round r1 exits 0
- Directory evals/cascade_audit/artifacts/round_1/ exists
- File parse_structured_fields_r1.json exists in round_1/
- File find_careers_url_r1.json exists in round_1/
- File extract_jobs_r1.json exists in round_1/
- File description_reformat_r1.json exists in round_1/
- File company_research_r1.json exists in round_1/
- File ai_nav_discovery_r1.json exists in round_1/
- Each artifact contains "sample_size" key with value 10
- Each artifact contains "accuracy" key
- Each artifact contains "latency_p50_ms" key
- Each artifact contains "cost_per_1k" key
- Each artifact contains "schema_valid_rate" key
</acceptance_criteria>

### Task 01-05: Execute R2 Head-to-Head Round

<read_first>
- evals/cascade_audit/harness.py (harness run_head_to_head method)
- evals/cascade_audit/run_audit.py (CLI entrypoint)
</read_first>

<action>
Execute R2 head-to-head round via CLI:

1. Run: uv run python -m evals.cascade_audit.run_audit --round r2
2. Verify harness completes without errors
3. Verify artifacts created in evals/cascade_audit/artifacts/round_2/
4. Verify each callsite has artifact with n=50 (objective) and n=100 (subjective) sample sizes per spec
5. Verify artifacts contain Wilson confidence intervals for binomial proportions
6. Verify "RESUME SCHEDULERS" prompt is emitted at end of R2
7. Check for borderline cases (measurements within 1 Wilson CI half-width of gate boundaries)
8. If borderline cases found, re-run with n=200 per D-05
9. If any catastrophic failure occurs, halt audit per D-01 fail-fast
</action>

<acceptance_criteria>
- Command uv run python -m evals.cascade_audit.run_audit --round r2 exits 0
- Directory evals/cascade_audit/artifacts/round_2/ exists
- File parse_structured_fields_r2.json exists in round_2/
- File find_careers_url_r2.json exists in round_2/
- File extract_jobs_r2.json exists in round_2/
- File description_reformat_r2.json exists in round_2/
- File company_research_r2.json exists in round_2/
- File ai_nav_discovery_r2.json exists in round_2/
- Each artifact contains "sample_size" key with value >= 50
- Each artifact contains "confidence_interval" key
- Each artifact contains "verdict" key with value SUITABLE, MARGINAL, or UNSUITABLE
- Standard output contains "RESUME SCHEDULERS"
</acceptance_criteria>

### Task 01-06: Spot-Check 10 Judge Verdicts

<read_first>
- evals/cascade_audit/artifacts/round_2/*.json (raw artifacts to inspect)
- .planning/specs/2026-05-13-local-cascade-audit-design.md (spec section 11 for spot-check criteria)
</read_first>

<action>
Perform manual spot-check of 10 judge verdicts:

1. Read 10 random judge verdicts from round_2 artifacts
2. For each verdict, compare candidate provider output vs Anthropic baseline
3. Check if judge's classification (SUITABLE/MARGINAL/UNSUITABLE) is reasonable
4. Record results in calibration log format:
   ```
   Check 1: parse_structured_fields / ollama vs anthropic - PASS
   Check 2: find_careers_url / groq vs anthropic - PASS
   ...
   ```
5. Count errors: >2 obvious errors per spec section 11 = calibration failure
6. If calibration fails, halt audit per D-01 and diagnose root cause
7. If ≤2 errors, calibration passes and continue to report generation
</action>

<acceptance_criteria>
- Calibration log contains 10 entries (Check 1 through Check 10)
- Each entry format: "Check N: {callsite} / {provider} vs anthropic - {PASS/FAIL}"
- Error count ≤ 2
- If error count > 2, audit halted with calibration failure message
</acceptance_criteria>

### Task 01-07: Generate CASCADE-AUDIT.md Report

<read_first>
- evals/cascade_audit/artifacts/round_2/*.json (raw artifacts to aggregate)
- .planning/specs/2026-05-13-local-cascade-audit-design.md (spec section 7 for report structure)
- .planning/phases/37-cascade-audit-execution-decision/37-CONTEXT.md (decision D-03 for Case A/B criteria)
</read_first>

<action>
Generate CASCADE-AUDIT.md at repo root with comprehensive report:

1. Create CASCADE-AUDIT.md in project root (not in .planning/)
2. Add Executive Summary section:
   - List 6 callsites audited
   - State Case A/B decision (explicit: "Case A" or "Case B")
   - Write recommended cascade ordering verbatim
3. Add Verdict Grid table with columns:
   - Callsite, Provider, Verdict, Sample Size, Confidence, Gates Failed
   - Fill with data from round_2 artifacts
4. Add Per-Callsite Recommendations section:
   - For each callsite, list recommended cascade ordering
   - Include rationale from R2 results
5. Add Calibration Log section:
   - Record 10 spot-check results from Task 01-06
   - State result: "X/10 passed (≤2 errors threshold met)"
6. Add Case A/B Decision section:
   - Explicitly state "Case A (single shared cascade)" or "Case B (purpose_overrides)"
   - If Case B, list purpose_overrides entries per D-03
   - Write chosen cascade ordering verbatim
7. Add Risk Callouts section:
   - Document any MARGINAL verdicts with warnings per D-02
   - Document any borderline re-runs performed per D-05
8. Ensure all sections from spec section 7 are present
</action>

<acceptance_criteria>
- File CASCADE-AUDIT.md exists at repo root
- CASCADE-AUDIT.md contains "## Executive Summary"
- CASCADE-AUDIT.md contains "## Verdict Grid"
- CASCADE-AUDIT.md contains "| Callsite | Provider | Verdict |"
- CASCADE-AUDIT.md contains "## Per-Callsite Recommendations"
- CASCADE-AUDIT.md contains "## Calibration Log"
- CASCADE-AUDIT.md contains "Check 1:" through "Check 10:"
- CASCADE-AUDIT.md contains "## Case A/B Decision"
- CASCADE-AUDIT.md contains "Case A" or "Case B" explicitly
- CASCADE-AUDIT.md contains "purpose_overrides" if Case B
- CASCADE-AUDIT.md contains "single shared cascade" if Case A
- CASCADE-AUDIT.md contains recommended cascade ordering verbatim
</acceptance_criteria>

### Task 01-08: Create Integration Tests

<read_first>
- tests/test_audit_harness.py (Phase 36 test pattern)
- evals/cascade_audit/run_audit.py (CLI to test)
- CASCADE-AUDIT.md (report to validate)
</read_first>

<action>
Create tests/test_audit_execution.py with integration tests:

1. Create test file tests/test_audit_execution.py
2. Add fixture mock_harness that creates temporary artifact directory
3. Add test_round_execution():
   - Mock harness dependencies
   - Execute round via CLI (simulate run_audit.py call)
   - Verify artifacts created in correct round directory
4. Add test_cascade_audit_md_generation():
   - Generate CASCADE-AUDIT.md from mock artifacts
   - Verify file exists
   - Verify required sections present (grep for section headers)
5. Add test_case_a_b_decision_explicit():
   - Parse CASCADE-AUDIT.md
   - Verify "Case A" or "Case B" present
   - Verify "purpose_overrides" or "single shared cascade" present
6. Add test_calibration_log_format():
   - Verify calibration log has 10 entries
   - Verify each entry matches "Check N: ... - PASS/FAIL" pattern
7. Ensure tests use pytest fixtures and follow Phase 36 test patterns
</action>

<acceptance_criteria>
- File tests/test_audit_execution.py exists
- test_audit_execution.py contains def test_round_execution
- test_audit_execution.py contains def test_cascade_audit_md_generation
- test_audit_execution.py contains def test_case_a_b_decision_explicit
- test_audit_execution.py contains def test_calibration_log_format
- test_audit_execution.py contains @pytest.fixture
- Command uv run --active pytest tests/test_audit_execution.py -v exits 0
</acceptance_criteria>

### Task 01-09: Run Test Suite

<read_first>
- tests/test_audit_execution.py (test file to run)
- pyproject.toml (pytest configuration)
</read_first>

<action>
Run pytest on audit execution tests:

1. Run: uv run --active pytest tests/test_audit_execution.py -v
2. Verify all tests pass (exit code 0)
3. If any test fails, fix the implementation and re-run
4. Ensure test coverage for all audit execution paths
</action>

<acceptance_criteria>
- Command uv run --active pytest tests/test_audit_execution.py -v exits 0
- All test functions show PASSED status
- No test failures or errors
</acceptance_criteria>

### Task 01-10: Verify Audit Completeness

<read_first>
- CASCADE-AUDIT.md (report to verify)
- evals/cascade_audit/artifacts/ (raw artifacts to verify)
- .planning/ROADMAP.md (Phase 37 success criteria)
</read_first>

<action>
Verify audit completeness against Phase 37 success criteria:

1. Verify R0/R1/R2 rounds executed:
   - Check round_0/, round_1/, round_2/ directories exist
   - Check each has 6 callsite artifacts
2. Verify CASCADE-AUDIT.md exists at repo root (not in .planning/)
3. Verify CASCADE-AUDIT.md summarizes per-callsite verdicts with sample sizes + confidence
4. Verify CASCADE-AUDIT.md renders recommended cascade for each callsite
5. Verify calibration log records 10 spot-checks with ≤2 errors
6. Verify Case A/B decision is explicit with cascade ordering written verbatim
7. If any criterion fails, fix and re-verify
</action>

<acceptance_criteria>
- Directory evals/cascade_audit/artifacts/round_0/ exists
- Directory evals/cascade_audit/artifacts/round_1/ exists
- Directory evals/cascade_audit/artifacts/round_2/ exists
- CASCADE-AUDIT.md exists at repo root
- CASCADE-AUDIT.md contains sample sizes for each verdict
- CASCADE-AUDIT.md contains confidence intervals
- CASCADE-AUDIT.md contains recommended cascade for each callsite
- CASCADE-AUDIT.md contains calibration log with 10 entries
- CASCADE-AUDIT.md calibration log shows ≤2 errors
- CASCADE-AUDIT.md contains explicit Case A or Case B decision
- CASCADE-AUDIT.md contains cascade ordering verbatim
</acceptance_criteria>

## Verification

1. Run audit execution tests: `uv run --active pytest tests/test_audit_execution.py -v`
2. Verify CASCADE-AUDIT.md exists at repo root with all required sections
3. Verify raw artifacts exist in evals/cascade_audit/artifacts/round_N/
4. Verify Case A/B decision is explicit with cascade ordering
5. Verify calibration log shows ≤2 errors in 10 spot-checks

## Success Criteria

- [ ] R0 calibration / R1 contenders / R2 head-to-head rounds executed
- [ ] Raw artifacts present in evals/cascade_audit/artifacts/ (gitignored)
- [ ] CASCADE-AUDIT.md exists at repo root
- [ ] CASCADE-AUDIT.md summarizes per-callsite verdicts with sample sizes + confidence
- [ ] CASCADE-AUDIT.md renders recommended cascade for each callsite
- [ ] User spot-checked 10 judge verdicts; ≤2 obvious errors
- [ ] Calibration log recorded in CASCADE-AUDIT.md
- [ ] Case A/B decision is explicit in CASCADE-AUDIT.md
- [ ] Cascade ordering written verbatim in CASCADE-AUDIT.md
- [ ] Tests pass: uv run --active pytest tests/test_audit_execution.py -v
