---
phase: 22-module-splits
plan: 04
type: execute
wave: 2
depends_on: [22-01]
files_modified:
  - job_finder/web/data_enricher.py
  - job_finder/web/enrichment_tiers.py
  - job_finder/web/company_enricher.py
  - job_finder/web/backfill_companies.py
  - job_finder/web/pipeline_runner.py
  - tests/test_data_enricher.py
autonomous: true
requirements: [SPLIT-02]

must_haves:
  truths:
    - "data_enricher.py, enrichment_tiers.py, and company_enricher.py each exist with cohesive scopes"
    - "All existing data enricher tests pass with updated imports"
    - "No circular imports at module load time"
  artifacts:
    - path: "job_finder/web/enrichment_tiers.py"
      provides: "Individual enrichment tier implementations (direct JD, ATS API, scraping, AI extraction, search)"
      exports: ["fetch_direct_jd", "query_ats_api", "scrape_careers", "extract_with_sonnet", "search_serpapi", "search_duckduckgo", "extract_with_haiku"]
      contains: "fetch_direct_jd"
    - path: "job_finder/web/company_enricher.py"
      provides: "Company info enrichment via web search"
      exports: ["enrich_company_info"]
      contains: "enrich_company_info"
    - path: "job_finder/web/data_enricher.py"
      provides: "Enrichment orchestration and backfill"
      exports: ["enrich_job", "run_enrichment_backfill"]
  key_links:
    - from: "job_finder/web/data_enricher.py"
      to: "job_finder/web/enrichment_tiers.py"
      via: "import tier functions"
      pattern: "from job_finder.web.enrichment_tiers import fetch_direct_jd"
    - from: "job_finder/web/company_enricher.py"
      to: "job_finder/web/enrichment_tiers.py"
      via: "import search_duckduckgo"
      pattern: "from job_finder.web.enrichment_tiers import search_duckduckgo"
    - from: "job_finder/web/pipeline_runner.py"
      to: "job_finder/web/company_enricher.py"
      via: "import enrich_company_info"
      pattern: "from job_finder.web.company_enricher import enrich_company_info"
---

<objective>
Split data_enricher.py (1112 LOC) into three focused modules: data_enricher.py (orchestration), enrichment_tiers.py (individual tier implementations), and company_enricher.py (company info enrichment).

Purpose: Separate orchestration from individual data source implementations.
Output: Three focused modules, updated callers and test imports, all existing tests passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-module-splits/22-RESEARCH.md

@job_finder/web/data_enricher.py
@job_finder/web/pipeline_runner.py
@job_finder/web/backfill_companies.py
@tests/test_data_enricher.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract enrichment_tiers.py and company_enricher.py from data_enricher.py</name>
  <files>
    job_finder/web/enrichment_tiers.py
    job_finder/web/company_enricher.py
    job_finder/web/data_enricher.py
  </files>
  <read_first>
    job_finder/web/data_enricher.py
    .planning/phases/22-module-splits/22-RESEARCH.md (SPLIT-02 section)
  </read_first>
  <action>
    **Create `job_finder/web/enrichment_tiers.py`** (~480 LOC):
    - Move constants: `_DDG_API_URL`, `_SERPAPI_URL`, `_HEADERS`, `_TIMEOUT`, `_MAX_JD_CHARS`, `_AUTH_WALL_SIGNATURES`, `_NOISE_TAGS` (from ~lines 68-100)
    - Move and **rename** (drop leading underscore) these functions:
      - `_fetch_direct_jd()` -> `fetch_direct_jd()`
      - `_query_ats_api()` -> `query_ats_api()`
      - `_scrape_careers()` -> `scrape_careers()`
      - `_extract_with_sonnet()` -> `extract_with_sonnet()`
      - `_search_serpapi()` -> `search_serpapi()`
      - `_search_duckduckgo()` -> `search_duckduckgo()`
      - `_extract_with_haiku()` -> `extract_with_haiku()`
    - Imports needed: `json`, `logging`, `re`, `Optional`/`Any` from typing, `requests`, `BeautifulSoup` from bs4, `DEFAULT_MODEL_HAIKU`/`DEFAULT_MODEL_SONNET` from `job_finder.config`, `call_claude`/`cost_gate` from `job_finder.web.claude_client`
    - Add module docstring: "Individual enrichment tier implementations for job data extraction."
    - Add `logger = logging.getLogger(__name__)`

    **Create `job_finder/web/company_enricher.py`** (~70 LOC):
    - Move `enrich_company_info()` (lines 339-398) from `data_enricher.py`
    - Imports needed: `logging`, `re`, `search_duckduckgo` from `job_finder.web.enrichment_tiers`
    - Add module docstring: "Company information enrichment via web search."
    - Add `logger = logging.getLogger(__name__)`

    **Update `job_finder/web/data_enricher.py`**:
    - Remove all moved constants and functions
    - Add imports at top:
      ```python
      from job_finder.web.enrichment_tiers import (
          fetch_direct_jd, query_ats_api, scrape_careers,
          extract_with_sonnet, search_serpapi, search_duckduckgo, extract_with_haiku,
      )
      from job_finder.web.company_enricher import enrich_company_info
      ```
    - Update all call sites in `enrich_job()` to use the new public names (no leading underscore):
      - `_fetch_direct_jd(...)` -> `fetch_direct_jd(...)`
      - `_query_ats_api(...)` -> `query_ats_api(...)`
      - `_scrape_careers(...)` -> `scrape_careers(...)`
      - `_extract_with_sonnet(...)` -> `extract_with_sonnet(...)`
      - `_search_serpapi(...)` -> `search_serpapi(...)`
      - `_search_duckduckgo(...)` -> `search_duckduckgo(...)`
      - `_extract_with_haiku(...)` -> `extract_with_haiku(...)`
    - Keep: `TIER_ORDER`, `FIELD_TIER_CEILINGS`, `_ENRICHABLE_COLUMNS` constants, `enrich_job()`, `run_enrichment_backfill()`, and all private helpers (`_find_missing_fields`, `_filter_non_none`, `_parse_salary_string`, `_start_tier_index`, `_parse_source_urls`, `_compose_fragment_text`, `_resolve_from_fragments`, `_persist`)
  </action>
  <verify>
    <automated>cd /c/Users/senki/repos/job-cannon && uv run python -c "from job_finder.web.enrichment_tiers import fetch_direct_jd, search_serpapi; from job_finder.web.company_enricher import enrich_company_info; from job_finder.web.data_enricher import enrich_job, run_enrichment_backfill; print('All imports OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `job_finder/web/enrichment_tiers.py` exists and exports all 7 tier functions with public names (no underscore prefix)
    - `job_finder/web/company_enricher.py` exists and exports `enrich_company_info`
    - `data_enricher.py` imports from both new modules and calls functions by new public names
    - No `ImportError` when importing any of the three modules
  </acceptance_criteria>
  <done>Three focused modules exist, all importable without circular dependency errors</done>
</task>

<task type="auto">
  <name>Task 2: Update callers and test imports for SPLIT-02</name>
  <files>
    job_finder/web/backfill_companies.py
    job_finder/web/pipeline_runner.py
    tests/test_data_enricher.py
  </files>
  <read_first>
    job_finder/web/backfill_companies.py
    job_finder/web/pipeline_runner.py
    tests/test_data_enricher.py
    .planning/phases/22-module-splits/22-RESEARCH.md (SPLIT-02 External Importer Updates section)
  </read_first>
  <action>
    **Update `job_finder/web/backfill_companies.py`**:
    - Find: `from job_finder.web.data_enricher import enrich_company_info` (around line 42)
    - Change to: `from job_finder.web.company_enricher import enrich_company_info`

    **Update `job_finder/web/pipeline_runner.py`**:
    - Find the import of `enrich_company_info` from `data_enricher` (around line 48)
    - Change to: `from job_finder.web.company_enricher import enrich_company_info`
    - Keep `enrich_job` import from `data_enricher` (it stays there)
    - Note: `pipeline_runner.py` was already modified by Plan 22-01 (SPLIT-01) which updated the `extract_ats_from_urls` import. Read the current state of the file first.

    **Update `tests/test_data_enricher.py`**:
    - Find imports of private helpers from `job_finder.web.data_enricher`:
      - `_search_serpapi` -> now `search_serpapi` from `job_finder.web.enrichment_tiers`
      - `_search_duckduckgo` -> now `search_duckduckgo` from `job_finder.web.enrichment_tiers`
      - `_extract_with_haiku` -> now `extract_with_haiku` from `job_finder.web.enrichment_tiers`
      - `_fetch_direct_jd` -> now `fetch_direct_jd` from `job_finder.web.enrichment_tiers`
      - `enrich_company_info` -> now from `job_finder.web.company_enricher`
    - Keep imports from `data_enricher` for: `enrich_job`, `TIER_ORDER`, and any other symbols that stayed
    - Update ALL monkeypatch targets accordingly:
      - Any `monkeypatch.setattr` that patches `data_enricher._search_serpapi` must now patch `enrichment_tiers.search_serpapi`
      - Same for all other moved+renamed functions
      - Import the `enrichment_tiers` module for patching: `import job_finder.web.enrichment_tiers as tiers_mod`
      - Import `company_enricher` module: `import job_finder.web.company_enricher as company_enricher_mod`
    - Update function name references in test code: `_search_serpapi` -> `search_serpapi` etc.

    **Do NOT update** these files (imports unchanged per research):
    - `backfill_enrichment.py` (imports `enrich_job` — stays in `data_enricher`)
    - `startup_backfills.py` (imports `run_enrichment_backfill` — stays in `data_enricher`)
  </action>
  <verify>
    <automated>cd /c/Users/senki/repos/job-cannon && uv run pytest tests/test_data_enricher.py tests/test_backfill_enrichment.py tests/test_backfill_companies.py -x --tb=short -q</automated>
  </verify>
  <acceptance_criteria>
    - `backfill_companies.py` imports `enrich_company_info` from `company_enricher`
    - `pipeline_runner.py` imports `enrich_company_info` from `company_enricher`
    - `test_data_enricher.py` imports tier functions from `enrichment_tiers` with public names
    - All monkeypatch targets point to correct module for moved functions
    - All data enricher, backfill enrichment, and backfill companies tests pass
    - Full test suite: `uv run pytest tests/ -x --tb=short -q` passes
  </acceptance_criteria>
  <done>All callers and test imports updated; full test suite green</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/ -x --tb=short -q` — full suite passes with no regressions
- `wc -l job_finder/web/data_enricher.py job_finder/web/enrichment_tiers.py job_finder/web/company_enricher.py` — combined LOC does not exceed pre-split 1112
</verification>

<success_criteria>
- Three focused modules exist with cohesive scopes
- All tests pass with no regressions
- No circular import errors at startup
</success_criteria>

<output>
After completion, create `.planning/phases/22-module-splits/22-04-SUMMARY.md`
</output>
