---
phase: 22-module-splits
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - job_finder/web/ats_scanner.py
  - job_finder/web/ats_detection.py
  - job_finder/web/ats_prober.py
  - job_finder/web/pipeline_runner.py
  - job_finder/web/blueprints/companies.py
  - tests/test_ats_scanner.py
autonomous: true
requirements: [SPLIT-01]

must_haves:
  truths:
    - "ats_detection.py, ats_prober.py, and ats_scanner.py each exist with their designated responsibilities"
    - "All existing ats_scanner tests pass without modification to test logic (only import paths change)"
    - "No circular imports at module load time"
  artifacts:
    - path: "job_finder/web/ats_detection.py"
      provides: "ATS URL extraction and slug derivation"
      exports: ["extract_ats_from_urls", "derive_slug_candidates"]
      contains: "_LEVER_JOBS_URL"
    - path: "job_finder/web/ats_prober.py"
      provides: "Single-company ATS probing with retry/backoff"
      exports: ["probe_single_company"]
      contains: "probe_single_company"
    - path: "job_finder/web/ats_scanner.py"
      provides: "ATS scanning orchestration, company upsert, slug probing"
      exports: ["upsert_company", "probe_ats_slugs", "run_ats_scan"]
  key_links:
    - from: "job_finder/web/ats_prober.py"
      to: "job_finder/web/ats_detection.py"
      via: "import derive_slug_candidates"
      pattern: "from job_finder.web.ats_detection import derive_slug_candidates"
    - from: "job_finder/web/pipeline_runner.py"
      to: "job_finder/web/ats_detection.py"
      via: "lazy import extract_ats_from_urls"
      pattern: "from job_finder.web.ats_detection import extract_ats_from_urls"
    - from: "job_finder/web/blueprints/companies.py"
      to: "job_finder/web/ats_prober.py"
      via: "import probe_single_company"
      pattern: "from job_finder.web.ats_prober import probe_single_company"
---

<objective>
Split ats_scanner.py (1475 LOC) into three focused modules: ats_detection.py (URL extraction + slug derivation), ats_prober.py (single-company ATS probing), and ats_scanner.py (scanning orchestration).

Purpose: Decompose god-object module along responsibility boundaries for faster navigation and cleaner import graph.
Output: Three focused modules, updated callers, all existing tests passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-module-splits/22-RESEARCH.md

@job_finder/web/ats_scanner.py
@job_finder/web/pipeline_runner.py
@job_finder/web/blueprints/companies.py
@tests/test_ats_scanner.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract ats_detection.py and ats_prober.py from ats_scanner.py</name>
  <files>
    job_finder/web/ats_detection.py
    job_finder/web/ats_prober.py
    job_finder/web/ats_scanner.py
  </files>
  <read_first>
    job_finder/web/ats_scanner.py
    .planning/phases/22-module-splits/22-RESEARCH.md (SPLIT-01 section)
  </read_first>
  <action>
    **Create `job_finder/web/ats_detection.py`** (~120 LOC):
    - Move the 5 ATS URL regex constants: `_LEVER_JOBS_URL`, `_LEVER_API_URL`, `_GREENHOUSE_BOARDS_URL`, `_GREENHOUSE_API_URL`, `_ASHBY_URL` (lines 69-92 of ats_scanner.py)
    - Move `extract_ats_from_urls()` (lines 445-484)
    - Move `derive_slug_candidates()` (lines 486-524)
    - Imports needed: `re`, `logging`, `Optional` from typing
    - Add module docstring: "ATS URL pattern extraction and slug candidate derivation."
    - Add `logger = logging.getLogger(__name__)`

    **Create `job_finder/web/ats_prober.py`** (~330 LOC):
    - Move constants: `_PROBE_STATUS_PRECEDENCE`, `_BACKOFF_HOURS`, `_MAX_RETRIES`, `_TRANSIENT_CODES`, `_PERMANENT_MISS_CODES` (lines 97-116)
    - Move functions: `_compute_retry_after()`, `_is_transient_error()`, `_handle_scan_error()`, `_reset_retry_state()`, `probe_single_company()`, `_probe_lever_with_result()`, `_probe_lever()`, `_probe_greenhouse()`, `_probe_ashby()`
    - Imports needed: `sqlite3`, `logging`, `time`, `datetime/timedelta/timezone` from datetime, `requests`, `standalone_connection` from `job_finder.web.db_helpers`, `derive_slug_candidates` from `job_finder.web.ats_detection`
    - Also needs `_PROBE_TIMEOUT` — copy this constant (value: 8) into ats_prober.py since probing functions use it
    - Add module docstring: "Single-company ATS probing with retry, backoff, and error handling."
    - Add `logger = logging.getLogger(__name__)`

    **Update `job_finder/web/ats_scanner.py`**:
    - Remove all moved constants, functions
    - Add re-exports at top of file (after existing imports):
      ```python
      from job_finder.web.ats_detection import extract_ats_from_urls, derive_slug_candidates
      from job_finder.web.ats_prober import probe_single_company, _probe_lever, _probe_greenhouse, _probe_ashby
      ```
    - Keep `_PROBE_TIMEOUT` if any remaining scanning functions use it (verify); if only probing functions used it, remove it
    - Keep `_title_matches()`, `upsert_company()`, `probe_ats_slugs()`, `scan_lever()`, `scan_greenhouse()`, `scan_ashby()`, `run_ats_scan()` and all their supporting constants/imports
    - Verify the remaining ats_scanner.py does NOT import the moved regex constants (it shouldn't need them after the move)
  </action>
  <verify>
    <automated>cd /c/Users/senki/repos/job-cannon && uv run python -c "from job_finder.web.ats_detection import extract_ats_from_urls, derive_slug_candidates; from job_finder.web.ats_prober import probe_single_company; from job_finder.web.ats_scanner import upsert_company, run_ats_scan; print('All imports OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `job_finder/web/ats_detection.py` exists and exports `extract_ats_from_urls` and `derive_slug_candidates`
    - `job_finder/web/ats_prober.py` exists and exports `probe_single_company`
    - `job_finder/web/ats_scanner.py` re-exports moved symbols for backward compatibility
    - No `ImportError` when importing any of the three modules
  </acceptance_criteria>
  <done>Three focused modules exist, all importable without circular dependency errors</done>
</task>

<task type="auto">
  <name>Task 2: Update callers and test imports for SPLIT-01</name>
  <files>
    job_finder/web/pipeline_runner.py
    job_finder/web/blueprints/companies.py
    tests/test_ats_scanner.py
  </files>
  <read_first>
    job_finder/web/pipeline_runner.py
    job_finder/web/blueprints/companies.py
    tests/test_ats_scanner.py
    .planning/phases/22-module-splits/22-RESEARCH.md (SPLIT-01 External Importer Updates section)
  </read_first>
  <action>
    **Update `job_finder/web/pipeline_runner.py`**:
    - Find the lazy import block (around line 326) that does:
      ```python
      from job_finder.web.ats_scanner import extract_ats_from_urls, upsert_company
      ```
    - Change `extract_ats_from_urls` import to: `from job_finder.web.ats_detection import extract_ats_from_urls`
    - Keep `upsert_company` import from `ats_scanner` (it stays there)
    - The ImportError guard pattern must remain intact

    **Update `job_finder/web/blueprints/companies.py`**:
    - Find the import (around line 25): `from job_finder.web.ats_scanner import probe_single_company`
    - Change to: `from job_finder.web.ats_prober import probe_single_company`

    **Update `tests/test_ats_scanner.py`**:
    - Find imports of `extract_ats_from_urls` from `job_finder.web.ats_scanner`
    - Change to: `from job_finder.web.ats_detection import extract_ats_from_urls`
    - Keep `_title_matches` and `upsert_company` imports from `ats_scanner` (they stay there)
    - Update any monkeypatch targets that reference moved functions — patch at the new module location (e.g., `job_finder.web.ats_detection.extract_ats_from_urls` not `job_finder.web.ats_scanner.extract_ats_from_urls`)

    **Do NOT update** these files (their imports stay unchanged per research):
    - `data_enricher.py` (imports `scan_lever`, `scan_greenhouse`, `scan_ashby` — stay in ats_scanner)
    - `careers_scraper.py` (imports `_title_matches` — stays in ats_scanner)
    - `expiry_checker.py` (imports `_title_matches` — stays in ats_scanner)
    - `backfill_companies.py` (imports `probe_ats_slugs`, `upsert_company` — stay in ats_scanner)
    - `scheduler.py` (imports `run_ats_scan`, `probe_ats_slugs` — stay in ats_scanner)
  </action>
  <verify>
    <automated>cd /c/Users/senki/repos/job-cannon && uv run pytest tests/test_ats_scanner.py tests/test_companies.py -x --tb=short -q</automated>
  </verify>
  <acceptance_criteria>
    - `pipeline_runner.py` imports `extract_ats_from_urls` from `ats_detection`, not `ats_scanner`
    - `companies.py` imports `probe_single_company` from `ats_prober`, not `ats_scanner`
    - `test_ats_scanner.py` imports `extract_ats_from_urls` from `ats_detection`
    - All ats_scanner tests pass
    - All companies tests pass
    - Full test suite: `uv run pytest tests/ -x --tb=short -q` passes
  </acceptance_criteria>
  <done>All callers updated to import from new module locations; full test suite green</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/ -x --tb=short -q` — full suite passes with no regressions
- `wc -l job_finder/web/ats_detection.py job_finder/web/ats_prober.py job_finder/web/ats_scanner.py` — combined LOC does not exceed pre-split 1475
- No `from job_finder.web.ats_scanner import extract_ats_from_urls` remaining in non-ats_scanner files (except re-export in ats_scanner itself)
</verification>

<success_criteria>
- Three focused modules exist with clear responsibility boundaries
- All 1562+ tests pass with no regressions
- No circular import errors at startup
</success_criteria>

<output>
After completion, create `.planning/phases/22-module-splits/22-01-SUMMARY.md`
</output>
