---
phase: 22-module-splits
plan: 05
type: execute
wave: 2
depends_on: [22-03]
files_modified:
  - job_finder/web/blueprints/dashboard.py
  - job_finder/web/blueprints/batch_scoring.py
  - job_finder/web/blueprints/sync.py
  - job_finder/web/__init__.py
  - tests/test_scoring.py
autonomous: true
requirements: [SPLIT-03]

must_haves:
  truths:
    - "dashboard.py, batch_scoring.py, and sync.py blueprints are all registered in create_app()"
    - "All dashboard, batch scoring, and sync routes respond correctly"
    - "All existing scoring and sync tests pass"
  artifacts:
    - path: "job_finder/web/blueprints/batch_scoring.py"
      provides: "Batch Haiku/Sonnet scoring start, status, cancel routes and background threads"
      exports: ["batch_scoring_bp"]
      contains: "batch_scoring_bp = Blueprint"
    - path: "job_finder/web/blueprints/sync.py"
      provides: "Gmail sync start, status, dismiss routes and background thread"
      exports: ["sync_bp"]
      contains: "sync_bp = Blueprint"
    - path: "job_finder/web/blueprints/dashboard.py"
      provides: "Dashboard index, cost detail, rejection analysis routes"
      exports: ["dashboard_bp"]
    - path: "job_finder/web/__init__.py"
      provides: "Blueprint registration for batch_scoring_bp and sync_bp"
      contains: "register_blueprint(batch_scoring_bp)"
  key_links:
    - from: "job_finder/web/__init__.py"
      to: "job_finder/web/blueprints/batch_scoring.py"
      via: "blueprint registration"
      pattern: "from job_finder.web.blueprints.batch_scoring import batch_scoring_bp"
    - from: "job_finder/web/__init__.py"
      to: "job_finder/web/blueprints/sync.py"
      via: "blueprint registration"
      pattern: "from job_finder.web.blueprints.sync import sync_bp"
---

<objective>
Split dashboard.py (913 LOC) into three blueprints: dashboard.py (dashboard page + stats), batch_scoring.py (batch Haiku/Sonnet scoring), and sync.py (Gmail sync operations).

Purpose: Separate dashboard rendering from batch operations and sync functionality.
Output: Three registered blueprints, updated test imports, all existing tests passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-module-splits/22-RESEARCH.md

@job_finder/web/blueprints/dashboard.py
@job_finder/web/__init__.py
@tests/test_scoring.py
@tests/test_async_sync.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract batch_scoring.py and sync.py blueprints from dashboard.py</name>
  <files>
    job_finder/web/blueprints/batch_scoring.py
    job_finder/web/blueprints/sync.py
    job_finder/web/blueprints/dashboard.py
    job_finder/web/__init__.py
  </files>
  <read_first>
    job_finder/web/blueprints/dashboard.py
    job_finder/web/__init__.py
    .planning/phases/22-module-splits/22-RESEARCH.md (SPLIT-03 section)
  </read_first>
  <action>
    **Create `job_finder/web/blueprints/batch_scoring.py`** (~480 LOC):
    - Blueprint definition: `batch_scoring_bp = Blueprint("batch_scoring", __name__, url_prefix="/dashboard")`
    - Move from `dashboard.py` (change decorator from `@dashboard_bp.route` to `@batch_scoring_bp.route`):
      - `batch_score_haiku_start()` — route: POST `/batch-score/haiku/start`
      - `batch_score_sonnet_start()` — route: POST `/batch-score/sonnet/start`
      - `batch_score_status()` — route: GET `/batch-score/status/<int:session_id>`
      - `batch_score_cancel()` — route: POST `/batch-score/cancel/<int:session_id>`
      - `_run_batch_haiku_bg()` — background thread function
      - `_run_batch_sonnet_bg()` — background thread function
      - `_update_session_counter()` — helper
      - `_finish_session()` — helper
      - `_fail_session()` — helper
      - `_mark_session_error()` — helper
    - Copy all imports needed by the moved functions from the top of `dashboard.py`
    - Add module docstring: "Batch scoring blueprint -- Haiku/Sonnet batch scoring start, status, cancel routes."
    - Add `logger = logging.getLogger(__name__)`

    **Create `job_finder/web/blueprints/sync.py`** (~290 LOC):
    - Blueprint definition: `sync_bp = Blueprint("sync", __name__, url_prefix="/dashboard")`
    - Move from `dashboard.py` (change decorator from `@dashboard_bp.route` to `@sync_bp.route`):
      - `sync_start()` — route: POST `/sync/start`
      - `sync_status()` — route: GET `/sync/status/<int:session_id>`
      - `sync_dismiss()` — route: POST `/sync/dismiss`
      - `_run_sync_bg()` — background thread function
      - `sync()` — route: POST `/sync` (the "Sync Now" quick action)
    - Copy all imports needed by the moved functions
    - Add module docstring: "Sync blueprint -- Gmail sync start, status, dismiss routes."
    - Add `logger = logging.getLogger(__name__)`

    **Update `job_finder/web/blueprints/dashboard.py`**:
    - Remove all moved functions
    - Remove imports only used by moved functions
    - Keep: `_get_ats_context()`, `_get_rejection_context()`, `index()`, `cost_detail()`, `rejection_analysis()` and their supporting imports

    **Update `job_finder/web/__init__.py`**:
    - Note: `__init__.py` was already modified by Plan 22-03 (SPLIT-07 guidelines_bp). Read the current state first.
    - Add imports and registrations after the existing `dashboard_bp` registration:
      ```python
      from job_finder.web.blueprints.batch_scoring import batch_scoring_bp
      from job_finder.web.blueprints.sync import sync_bp
      ```
    - Register BEFORE `jobs_bp` (to prevent route shadowing — same precedent as companies_bp):
      ```python
      app.register_blueprint(batch_scoring_bp)
      app.register_blueprint(sync_bp)
      ```
    - Place these registrations right after `app.register_blueprint(dashboard_bp)` and before any catch-all blueprint
  </action>
  <verify>
    <automated>cd /c/Users/senki/repos/job-cannon && uv run python -c "from job_finder.web.blueprints.batch_scoring import batch_scoring_bp; from job_finder.web.blueprints.sync import sync_bp; from job_finder.web.blueprints.dashboard import dashboard_bp; print('All blueprints importable')"</automated>
  </verify>
  <acceptance_criteria>
    - `job_finder/web/blueprints/batch_scoring.py` exists with `batch_scoring_bp` Blueprint using `url_prefix="/dashboard"`
    - `job_finder/web/blueprints/sync.py` exists with `sync_bp` Blueprint using `url_prefix="/dashboard"`
    - Both new blueprints are registered in `create_app()` in `__init__.py`
    - `dashboard.py` only contains dashboard index, cost detail, and rejection analysis routes
    - No `ImportError` when importing any of the three blueprints
  </acceptance_criteria>
  <done>Three blueprints exist, all registered in create_app(), no import errors</done>
</task>

<task type="auto">
  <name>Task 2: Update test imports for SPLIT-03</name>
  <files>
    tests/test_scoring.py
    tests/test_async_sync.py
  </files>
  <read_first>
    tests/test_scoring.py
    tests/test_async_sync.py
    .planning/phases/22-module-splits/22-RESEARCH.md (SPLIT-03 Test Updates section)
  </read_first>
  <action>
    **Update `tests/test_scoring.py`**:
    - Find imports of `_run_batch_haiku_bg` from `job_finder.web.blueprints.dashboard` (lines 1897, 1940, 1968 per research)
    - Change to: `from job_finder.web.blueprints.batch_scoring import _run_batch_haiku_bg`
    - Check for any imports of `_run_batch_sonnet_bg`, `_update_session_counter`, `_finish_session`, `_fail_session`, `_mark_session_error` from dashboard
    - Update all such imports to reference `batch_scoring` module
    - Update monkeypatch targets: any `monkeypatch.setattr` targeting `dashboard` module for moved functions must target `batch_scoring` module instead

    **Check `tests/test_async_sync.py`**:
    - Check if it imports any moved sync functions directly from `dashboard`
    - If it imports `_run_sync_bg` or `sync_start` etc. from dashboard, update to `from job_finder.web.blueprints.sync import ...`
    - Routes tested via Flask test client (e.g., `client.post("/dashboard/sync/start")`) should NOT need changes since url_prefix is the same
    - Update monkeypatch targets for moved functions

    Run full test suite after changes.
  </action>
  <verify>
    <automated>cd /c/Users/senki/repos/job-cannon && uv run pytest tests/test_scoring.py tests/test_async_sync.py tests/test_views.py -x --tb=short -q</automated>
  </verify>
  <acceptance_criteria>
    - No test imports reference moved functions from the `dashboard` module
    - All monkeypatch targets point to correct module for moved functions
    - All scoring tests pass
    - All async sync tests pass
    - All views tests pass
    - Full test suite: `uv run pytest tests/ -x --tb=short -q` passes
  </acceptance_criteria>
  <done>All test imports updated; full test suite green</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/ -x --tb=short -q` — full suite passes with no regressions
- `wc -l job_finder/web/blueprints/dashboard.py job_finder/web/blueprints/batch_scoring.py job_finder/web/blueprints/sync.py` — combined LOC does not exceed pre-split 913
</verification>

<success_criteria>
- Three registered blueprints with clear responsibility boundaries
- All dashboard, batch scoring, and sync routes respond correctly
- All tests pass with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/22-module-splits/22-05-SUMMARY.md`
</output>
