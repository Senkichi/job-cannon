---
phase: 22-module-splits
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - job_finder/web/resume_generator.py
  - job_finder/web/resume_multi_version.py
  - tests/test_resume.py
autonomous: true
requirements: [SPLIT-04]

must_haves:
  truths:
    - "resume_generator.py and resume_multi_version.py each exist with cohesive scopes"
    - "Multi-version synthesis logic is fully in resume_multi_version.py"
    - "All existing resume tests pass without modification to test logic"
    - "No circular imports at module load time"
  artifacts:
    - path: "job_finder/web/resume_multi_version.py"
      provides: "Multi-version resume generation and variant synthesis"
      exports: ["generate_resume_multi"]
      contains: "_haiku_select_strategies"
    - path: "job_finder/web/resume_generator.py"
      provides: "Single resume generation, constants, formatting helpers"
      exports: ["generate_resume_single", "RESUME_SCHEMA", "STRATEGY_POOL"]
  key_links:
    - from: "job_finder/web/resume_multi_version.py"
      to: "job_finder/web/resume_generator.py"
      via: "import constants and helpers"
      pattern: "from job_finder.web.resume_generator import RESUME_SCHEMA"
    - from: "job_finder/web/resume_generator.py"
      to: "job_finder/web/resume_multi_version.py"
      via: "deferred import in _generate_resume_background"
      pattern: "from job_finder.web.resume_multi_version import generate_resume_multi"
---

<objective>
Split resume_generator.py (995 LOC) into two focused modules: resume_generator.py (single resume generation, constants, helpers) and resume_multi_version.py (multi-version generation and variant synthesis).

Purpose: Separate single-resume and multi-version concerns into focused files.
Output: Two focused modules, updated test imports, all existing tests passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-module-splits/22-RESEARCH.md

@job_finder/web/resume_generator.py
@tests/test_resume.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract resume_multi_version.py from resume_generator.py</name>
  <files>
    job_finder/web/resume_multi_version.py
    job_finder/web/resume_generator.py
  </files>
  <read_first>
    job_finder/web/resume_generator.py
    .planning/phases/22-module-splits/22-RESEARCH.md (SPLIT-04 section)
  </read_first>
  <action>
    **Create `job_finder/web/resume_multi_version.py`** (~385 LOC):
    - Move these functions from `resume_generator.py`:
      - `_haiku_select_strategies()` (lines 399-490)
      - `_generate_single_variant()` (lines 491-626)
      - `generate_resume_multi()` (lines 628-706)
      - `_synthesize_variants()` (lines 709-780)
    - Add imports at top:
      ```python
      from job_finder.web.resume_generator import (
          RESUME_SCHEMA,
          STRATEGY_POOL,
          _STRATEGY_DESCRIPTIONS,
          _RESUME_GUIDELINES,
          _SYSTEM_PROMPT,
          _format_education,
          _format_profile_positions,
          _get_accepted_preferences,
      )
      ```
    - Also import: `anthropic`, `json`, `logging`, `concurrent.futures`, `call_claude` and `cost_gate` from `claude_client`, `standalone_connection` from `db_helpers`, `DEFAULT_MODEL_HAIKU` and `DEFAULT_MODEL_SONNET` from `config`
    - Add module docstring: "Multi-version resume generation with strategy selection and variant synthesis."
    - Add `logger = logging.getLogger(__name__)`

    **Update `job_finder/web/resume_generator.py`**:
    - Remove the 4 moved functions
    - In `_generate_resume_background()`, replace the direct call to `generate_resume_multi(...)` with a **deferred import** inside the function body (avoids circular import at module load time):
      ```python
      def _generate_resume_background(...):
          from job_finder.web.resume_multi_version import generate_resume_multi
          # ... rest of function using generate_resume_multi(...)
      ```
    - Keep all constants: `RESUME_SCHEMA`, `STRATEGY_POOL`, `_STRATEGY_DESCRIPTIONS`, `_RESUME_GUIDELINES`, `_SYSTEM_PROMPT`
    - Keep all helper functions: `_format_education()`, `_format_profile_positions()`, `_get_accepted_preferences()`
    - Keep `generate_resume_single()` and `_generate_resume_background()`

    **Do NOT change** these files (their imports are unaffected per research):
    - `job_finder/web/blueprints/resume.py` (imports `_generate_resume_background`, `generate_resume_single` — both stay)
    - `job_finder/web/resume_validator.py` (imports `RESUME_SCHEMA` — stays)
  </action>
  <verify>
    <automated>cd /c/Users/senki/repos/job-cannon && uv run python -c "from job_finder.web.resume_multi_version import generate_resume_multi; from job_finder.web.resume_generator import generate_resume_single, RESUME_SCHEMA, STRATEGY_POOL; print('All imports OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `job_finder/web/resume_multi_version.py` exists and exports `generate_resume_multi`
    - `job_finder/web/resume_generator.py` no longer contains `_haiku_select_strategies`, `_generate_single_variant`, `generate_resume_multi`, `_synthesize_variants`
    - `resume_generator.py` uses deferred import of `generate_resume_multi` inside `_generate_resume_background()`
    - No `ImportError` when importing either module
  </acceptance_criteria>
  <done>Two focused modules exist, both importable without circular dependency errors</done>
</task>

<task type="auto">
  <name>Task 2: Update test imports for SPLIT-04</name>
  <files>
    tests/test_resume.py
  </files>
  <read_first>
    tests/test_resume.py
    .planning/phases/22-module-splits/22-RESEARCH.md (SPLIT-04 Test Updates section)
  </read_first>
  <action>
    **Update `tests/test_resume.py`**:
    - Find imports of these symbols from `job_finder.web.resume_generator`:
      - `STRATEGY_POOL` — stays in `resume_generator` (no change)
      - `_haiku_select_strategies` — moved to `resume_multi_version`
      - `_generate_single_variant` — moved to `resume_multi_version`
      - `_synthesize_variants` — moved to `resume_multi_version`
      - `generate_resume_multi` — moved to `resume_multi_version`
    - Update those 4 imports to: `from job_finder.web.resume_multi_version import _haiku_select_strategies, _generate_single_variant, _synthesize_variants, generate_resume_multi`
    - Keep imports from `resume_generator` for: `RESUME_SCHEMA`, `_RESUME_GUIDELINES`, `_SYSTEM_PROMPT`, `_generate_resume_background`, `generate_resume_single`, `STRATEGY_POOL`
    - Update any monkeypatch targets that reference moved functions to patch at `job_finder.web.resume_multi_version` instead of `job_finder.web.resume_generator`
    - Pay special attention to monkeypatch.setattr calls that patch `generate_resume_multi`, `_haiku_select_strategies`, `_generate_single_variant`, or `_synthesize_variants` — these must target the new module
  </action>
  <verify>
    <automated>cd /c/Users/senki/repos/job-cannon && uv run pytest tests/test_resume.py tests/test_resume_validator.py -x --tb=short -q</automated>
  </verify>
  <acceptance_criteria>
    - `test_resume.py` imports moved functions from `resume_multi_version`, not `resume_generator`
    - All monkeypatch targets reference the correct module for moved functions
    - All resume tests pass
    - Full test suite: `uv run pytest tests/ -x --tb=short -q` passes
  </acceptance_criteria>
  <done>All test imports updated; full test suite green</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/ -x --tb=short -q` — full suite passes with no regressions
- `wc -l job_finder/web/resume_generator.py job_finder/web/resume_multi_version.py` — combined LOC does not exceed pre-split 995
</verification>

<success_criteria>
- Two focused modules exist with clear responsibility boundaries
- Multi-version synthesis logic is fully in resume_multi_version.py
- All tests pass with no regressions
- No circular import errors at startup
</success_criteria>

<output>
After completion, create `.planning/phases/22-module-splits/22-02-SUMMARY.md`
</output>
