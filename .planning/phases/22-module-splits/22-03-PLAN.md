---
phase: 22-module-splits
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - job_finder/web/blueprints/settings.py
  - job_finder/web/blueprints/guidelines.py
  - job_finder/web/__init__.py
  - tests/test_settings.py
  - tests/test_log_levels.py
autonomous: true
requirements: [SPLIT-07]

must_haves:
  truths:
    - "settings.py and guidelines.py blueprints are both registered in create_app()"
    - "All settings and guidelines routes respond correctly"
    - "All existing settings tests pass"
  artifacts:
    - path: "job_finder/web/blueprints/guidelines.py"
      provides: "Guidelines merge preview and apply routes"
      exports: ["guidelines_bp"]
      contains: "guidelines_bp = Blueprint"
    - path: "job_finder/web/blueprints/settings.py"
      provides: "Settings page and config save routes"
      exports: ["settings_bp"]
    - path: "job_finder/web/__init__.py"
      provides: "Blueprint registration for guidelines_bp"
      contains: "register_blueprint(guidelines_bp)"
  key_links:
    - from: "job_finder/web/__init__.py"
      to: "job_finder/web/blueprints/guidelines.py"
      via: "blueprint registration"
      pattern: "from job_finder.web.blueprints.guidelines import guidelines_bp"
---

<objective>
Split settings.py (621 LOC) into two blueprints: settings.py (settings page + config save) and guidelines.py (guidelines merge preview + apply routes).

Purpose: Separate config management from AI-powered guidelines merging into focused blueprints.
Output: Two registered blueprints, updated test imports, all existing tests passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-module-splits/22-RESEARCH.md

@job_finder/web/blueprints/settings.py
@job_finder/web/__init__.py
@tests/test_settings.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract guidelines.py blueprint from settings.py</name>
  <files>
    job_finder/web/blueprints/guidelines.py
    job_finder/web/blueprints/settings.py
    job_finder/web/__init__.py
  </files>
  <read_first>
    job_finder/web/blueprints/settings.py
    job_finder/web/__init__.py
    .planning/phases/22-module-splits/22-RESEARCH.md (SPLIT-07 section)
  </read_first>
  <action>
    **Create `job_finder/web/blueprints/guidelines.py`** (~240 LOC):
    - Blueprint definition: `guidelines_bp = Blueprint("guidelines", __name__, url_prefix="/settings")`
    - Move these route functions from `settings.py` (change decorator from `@settings_bp.route` to `@guidelines_bp.route`):
      - `migrate_style_guide_route()` (lines 170-206) — route: POST `/migrate-style-guide`
      - `preview_guidelines_merge()` (lines 209-345) — route: POST `/preview-guidelines-merge`
      - `apply_guidelines_merge()` (lines 346-398) — route: POST `/apply-guidelines-merge`
    - Add imports needed by moved functions: `anthropic`, `json`, `logging`, `flask` imports (`Blueprint`, `current_app`, `flash`, `redirect`, `render_template`, `request`, `url_for`), `load_style_guide`, `merge_guidelines_into_guide`, `save_style_guide`, `STYLE_GUIDE_SCHEMA`, `FIELD_LABELS` from `resume_style_guide`, `get_db` from `db_helpers`, `load_config` from `config`, `DEFAULT_MODEL_SONNET` from `config`
    - Define `_CONFIG_PATH = "config.yaml"` at module level (needed by `preview_guidelines_merge`)
    - Add module docstring: "Guidelines blueprint -- style guide migration and guidelines merge routes."
    - Add `logger = logging.getLogger(__name__)`

    **Update `job_finder/web/blueprints/settings.py`**:
    - Remove the 3 moved route functions
    - Remove imports that are ONLY used by moved functions (e.g., `anthropic` if no remaining code uses it, `migrate_style_guide` and `merge_guidelines_into_guide` from `resume_style_guide`)
    - Keep: `index()`, `save()`, `_deep_merge()`, `_parse_form_to_config()`, `_parse_serpapi_queries()`, `_write_config()` and all their supporting imports

    **Update `job_finder/web/__init__.py`**:
    - Add after existing `settings_bp` registration:
      ```python
      from job_finder.web.blueprints.guidelines import guidelines_bp
      ```
    - Add: `app.register_blueprint(guidelines_bp)` after `app.register_blueprint(settings_bp)`
  </action>
  <verify>
    <automated>cd /c/Users/senki/repos/job-cannon && uv run python -c "from job_finder.web.blueprints.guidelines import guidelines_bp; from job_finder.web.blueprints.settings import settings_bp; print('Both blueprints importable'); from job_finder.web import create_app; print('create_app importable')"</automated>
  </verify>
  <acceptance_criteria>
    - `job_finder/web/blueprints/guidelines.py` exists with `guidelines_bp` Blueprint using `url_prefix="/settings"`
    - `guidelines_bp` is registered in `create_app()` in `__init__.py`
    - `settings.py` no longer contains `migrate_style_guide_route`, `preview_guidelines_merge`, `apply_guidelines_merge`
    - No `ImportError` when importing either blueprint
  </acceptance_criteria>
  <done>Two blueprints exist, both registered in create_app(), no import errors</done>
</task>

<task type="auto">
  <name>Task 2: Update test imports and verify routes for SPLIT-07</name>
  <files>
    tests/test_settings.py
    tests/test_log_levels.py
  </files>
  <read_first>
    tests/test_settings.py
    tests/test_log_levels.py
    .planning/phases/22-module-splits/22-RESEARCH.md (SPLIT-07 Test Updates section)
  </read_first>
  <action>
    **Update `tests/test_settings.py`**:
    - Check for direct imports of moved functions (e.g., `from job_finder.web.blueprints.settings import preview_guidelines_merge`)
    - If found, update to: `from job_finder.web.blueprints.guidelines import ...`
    - Check for monkeypatch targets that reference `settings_mod.preview_guidelines_merge` or similar
    - If found, update to patch `guidelines_mod.preview_guidelines_merge` (import the guidelines module as `guidelines_mod`)
    - Routes tested via Flask test client (e.g., `client.post("/settings/preview-guidelines-merge")`) should NOT need changes since the url_prefix is the same (`/settings`)

    **Check `tests/test_log_levels.py`** (line 411):
    - It imports `from job_finder.web.blueprints import settings as settings_module`
    - Verify whether it accesses any moved functions — if it only checks logging configuration or imports the module, no change needed
    - If it accesses `migrate_style_guide_route`, `preview_guidelines_merge`, or `apply_guidelines_merge`, update the import and access

    Run full test suite after changes.
  </action>
  <verify>
    <automated>cd /c/Users/senki/repos/job-cannon && uv run pytest tests/test_settings.py tests/test_log_levels.py -x --tb=short -q</automated>
  </verify>
  <acceptance_criteria>
    - No test imports reference moved functions from the old `settings` module
    - All monkeypatch targets point to correct module for moved functions
    - All settings tests pass
    - All log_levels tests pass
    - Full test suite: `uv run pytest tests/ -x --tb=short -q` passes
  </acceptance_criteria>
  <done>All test imports updated; full test suite green</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/ -x --tb=short -q` — full suite passes with no regressions
- `wc -l job_finder/web/blueprints/settings.py job_finder/web/blueprints/guidelines.py` — combined LOC does not exceed pre-split 621
</verification>

<success_criteria>
- Two registered blueprints with clear responsibility boundaries
- All settings and guidelines routes respond correctly
- All tests pass with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/22-module-splits/22-03-SUMMARY.md`
</output>
