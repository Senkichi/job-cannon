---
phase: 22-module-splits
plan: 07
type: execute
wave: 3
depends_on: [22-04, 22-05]
files_modified:
  - job_finder/web/pipeline_runner.py
  - job_finder/web/scoring_runner.py
  - tests/test_data_enricher.py
  - tests/test_ingestion.py
  - tests/test_scoring.py
autonomous: true
requirements: [SPLIT-06]

must_haves:
  truths:
    - "pipeline_runner.py and scoring_runner.py each exist with scoring orchestration in the new module"
    - "All existing ingestion, scoring, and data enricher tests pass"
    - "No circular imports at module load time"
  artifacts:
    - path: "job_finder/web/scoring_runner.py"
      provides: "Haiku scoring and Sonnet evaluation orchestration"
      exports: ["run_haiku_scoring", "run_sonnet_evaluation"]
      contains: "run_haiku_scoring"
    - path: "job_finder/web/pipeline_runner.py"
      provides: "Pipeline ingestion orchestration, Gmail/SerpAPI fetch, budget alerts"
      exports: ["run_ingestion"]
  key_links:
    - from: "job_finder/web/pipeline_runner.py"
      to: "job_finder/web/scoring_runner.py"
      via: "import scoring functions"
      pattern: "from job_finder.web.scoring_runner import run_haiku_scoring, run_sonnet_evaluation"
---

<objective>
Split pipeline_runner.py (670 LOC) into two focused modules: pipeline_runner.py (ingestion orchestration) and scoring_runner.py (Haiku scoring + Sonnet evaluation orchestration).

Purpose: Separate ingestion pipeline from scoring orchestration concerns.
Output: Two focused modules, updated callers and test imports, all existing tests passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-module-splits/22-RESEARCH.md

@job_finder/web/pipeline_runner.py
@tests/test_data_enricher.py
@tests/test_ingestion.py
@tests/test_scoring.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract scoring_runner.py from pipeline_runner.py</name>
  <files>
    job_finder/web/scoring_runner.py
    job_finder/web/pipeline_runner.py
  </files>
  <read_first>
    job_finder/web/pipeline_runner.py
    .planning/phases/22-module-splits/22-RESEARCH.md (SPLIT-06 section)
  </read_first>
  <action>
    **Note:** `pipeline_runner.py` was modified by Plans 22-01 (SPLIT-01: extract_ats_from_urls import) and 22-04 (SPLIT-02: enrich_company_info import). Read the current state of the file first.

    **Create `job_finder/web/scoring_runner.py`** (~235 LOC):
    - Move and **rename** (drop leading underscore) from `pipeline_runner.py`:
      - `_run_haiku_scoring()` -> `run_haiku_scoring()` (lines 395-530 of original)
      - `_run_sonnet_evaluation()` -> `run_sonnet_evaluation()` (lines 533-628 of original)
    - Imports needed (check the function bodies for all references):
      - `anthropic` (with ImportError guard)
      - `logging`
      - `JOBS_ALL_COLUMNS` from `job_finder.db`
      - `DEFAULT_HAIKU_THRESHOLD` from `job_finder.config`
      - `standalone_connection` from `job_finder.web.db_helpers`
      - `score_job_haiku` from `job_finder.web.haiku_scorer`
      - `load_scoring_profile`, `score_and_persist_haiku`, `score_and_persist_sonnet` from `job_finder.web.scoring_orchestrator`
      - `evaluate_job_sonnet` from `job_finder.web.sonnet_evaluator` (ImportError guarded)
      - `enrich_job` from `job_finder.web.data_enricher` (ImportError guarded via try/except)
      - `enrich_company_info` from `job_finder.web.company_enricher` (ImportError guarded — note: this import path was updated by SPLIT-02)
    - **Do NOT move** module-level state: `_budget_alert_lock`, `_last_budget_pct_notified` stay in `pipeline_runner.py` (used by `_check_budget_alert()`, not by scoring functions)
    - Add module docstring: "Scoring runner -- Haiku batch scoring and Sonnet deep evaluation orchestration."
    - Add `logger = logging.getLogger(__name__)`

    **Update `job_finder/web/pipeline_runner.py`**:
    - Remove the 2 moved functions
    - Add import at top:
      ```python
      from job_finder.web.scoring_runner import run_haiku_scoring, run_sonnet_evaluation
      ```
    - Update calls within `run_ingestion()` from `_run_haiku_scoring(...)` to `run_haiku_scoring(...)` and `_run_sonnet_evaluation(...)` to `run_sonnet_evaluation(...)`
    - Keep: module-level state, `run_ingestion()`, `_fetch_gmail()`, `_fetch_serpapi()`, `_score_and_persist()`, `_log_to_email_parse_log()`, `_check_budget_alert()`
  </action>
  <verify>
    <automated>cd /c/Users/senki/repos/job-cannon && uv run python -c "from job_finder.web.scoring_runner import run_haiku_scoring, run_sonnet_evaluation; from job_finder.web.pipeline_runner import run_ingestion; print('All imports OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `job_finder/web/scoring_runner.py` exists and exports `run_haiku_scoring` and `run_sonnet_evaluation` (public names, no underscore)
    - `pipeline_runner.py` imports from `scoring_runner` and calls public function names
    - `pipeline_runner.py` no longer contains `_run_haiku_scoring` or `_run_sonnet_evaluation`
    - No `ImportError` when importing either module
  </acceptance_criteria>
  <done>Two focused modules exist, both importable without circular dependency errors</done>
</task>

<task type="auto">
  <name>Task 2: Update test imports for SPLIT-06</name>
  <files>
    tests/test_data_enricher.py
    tests/test_ingestion.py
    tests/test_scoring.py
  </files>
  <read_first>
    tests/test_data_enricher.py
    tests/test_ingestion.py
    tests/test_scoring.py
    .planning/phases/22-module-splits/22-RESEARCH.md (SPLIT-06 External Importer Updates section)
  </read_first>
  <action>
    **Note:** `tests/test_data_enricher.py` was modified by Plan 22-04 (SPLIT-02). `tests/test_scoring.py` was modified by Plan 22-05 (SPLIT-03). Read the current state of these files first.

    **Update `tests/test_data_enricher.py`** (lines 928, 989 per research):
    - Find imports of `_run_haiku_scoring` / `_run_sonnet_evaluation` from `job_finder.web.pipeline_runner`
    - Change to: `from job_finder.web.scoring_runner import run_haiku_scoring, run_sonnet_evaluation`
    - Update function name references in test code: `_run_haiku_scoring` -> `run_haiku_scoring`, `_run_sonnet_evaluation` -> `run_sonnet_evaluation`
    - Update monkeypatch targets to patch at `scoring_runner` module, not `pipeline_runner`

    **Update `tests/test_ingestion.py`** (lines 919, 958, 979 per research):
    - Find imports of `_run_haiku_scoring` / `_run_sonnet_evaluation` from `job_finder.web.pipeline_runner`
    - Change to: `from job_finder.web.scoring_runner import run_haiku_scoring, run_sonnet_evaluation`
    - Update function name references and monkeypatch targets

    **Update `tests/test_scoring.py`** (lines 1222-1808 per research):
    - Find imports of `_run_haiku_scoring` / `_run_sonnet_evaluation` from `job_finder.web.pipeline_runner`
    - Change to: `from job_finder.web.scoring_runner import run_haiku_scoring, run_sonnet_evaluation`
    - Update ALL function name references and monkeypatch targets throughout the file
    - There may be many occurrences — be thorough

    **Do NOT update** `tests/test_parsers.py` — it imports `_fetch_gmail` from `pipeline_runner` which stays there.

    Run full test suite after changes.
  </action>
  <verify>
    <automated>cd /c/Users/senki/repos/job-cannon && uv run pytest tests/test_ingestion.py tests/test_scoring.py tests/test_data_enricher.py -x --tb=short -q</automated>
  </verify>
  <acceptance_criteria>
    - No test imports reference `_run_haiku_scoring` or `_run_sonnet_evaluation` from `pipeline_runner`
    - All test imports use `run_haiku_scoring`/`run_sonnet_evaluation` from `scoring_runner` (public names)
    - All monkeypatch targets point to `scoring_runner` module for moved functions
    - All ingestion, scoring, and data enricher tests pass
    - Full test suite: `uv run pytest tests/ -x --tb=short -q` passes
  </acceptance_criteria>
  <done>All test imports updated; full test suite green</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/ -x --tb=short -q` — full suite passes with no regressions
- `wc -l job_finder/web/pipeline_runner.py job_finder/web/scoring_runner.py` — combined LOC does not exceed pre-split 670
</verification>

<success_criteria>
- Two focused modules exist with clear responsibility boundaries
- Scoring orchestration is fully in scoring_runner.py
- All tests pass with no regressions
- No circular import errors at startup
</success_criteria>

<output>
After completion, create `.planning/phases/22-module-splits/22-07-SUMMARY.md`
</output>
