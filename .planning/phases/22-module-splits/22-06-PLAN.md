---
phase: 22-module-splits
plan: 06
type: execute
wave: 3
depends_on: [22-03, 22-05]
files_modified:
  - job_finder/web/blueprints/profile.py
  - job_finder/web/blueprints/resume_review.py
  - job_finder/web/blueprints/profile_recommendations.py
  - job_finder/web/__init__.py
  - tests/test_profile.py
autonomous: true
requirements: [SPLIT-05]

must_haves:
  truths:
    - "profile.py, resume_review.py, and profile_recommendations.py blueprints are all registered in create_app()"
    - "All profile, resume review, and recommendation routes respond correctly"
    - "url_for references updated for moved routes (profile.conflict_review -> resume_review.conflict_review)"
    - "All existing profile tests pass"
  artifacts:
    - path: "job_finder/web/blueprints/resume_review.py"
      provides: "PDF upload, conflict review, style extraction routes"
      exports: ["resume_review_bp"]
      contains: "resume_review_bp = Blueprint"
    - path: "job_finder/web/blueprints/profile_recommendations.py"
      provides: "Profile recommendation, recommendations_all, apply_fix routes"
      exports: ["profile_recs_bp"]
      contains: "profile_recs_bp = Blueprint"
    - path: "job_finder/web/blueprints/profile.py"
      provides: "Profile index, save, import, reorder routes"
      exports: ["profile_bp"]
    - path: "job_finder/web/__init__.py"
      provides: "Blueprint registration for resume_review_bp and profile_recs_bp"
      contains: "register_blueprint(resume_review_bp)"
  key_links:
    - from: "job_finder/web/__init__.py"
      to: "job_finder/web/blueprints/resume_review.py"
      via: "blueprint registration"
      pattern: "from job_finder.web.blueprints.resume_review import resume_review_bp"
    - from: "job_finder/web/__init__.py"
      to: "job_finder/web/blueprints/profile_recommendations.py"
      via: "blueprint registration"
      pattern: "from job_finder.web.blueprints.profile_recommendations import profile_recs_bp"
    - from: "job_finder/web/blueprints/profile_recommendations.py"
      to: "job_finder/web/blueprints/profile.py"
      via: "import _get_all_skills"
      pattern: "from job_finder.web.blueprints.profile import _get_all_skills"
---

<objective>
Split profile.py (913 LOC) into three blueprints: profile.py (profile management), resume_review.py (PDF upload + conflict review + style extraction), and profile_recommendations.py (recommendations + apply_fix).

Purpose: Separate core profile management from resume review and AI recommendation concerns.
Output: Three registered blueprints, updated url_for references, updated test imports, all existing tests passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-module-splits/22-RESEARCH.md

@job_finder/web/blueprints/profile.py
@job_finder/web/__init__.py
@tests/test_profile.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract resume_review.py and profile_recommendations.py blueprints from profile.py</name>
  <files>
    job_finder/web/blueprints/resume_review.py
    job_finder/web/blueprints/profile_recommendations.py
    job_finder/web/blueprints/profile.py
    job_finder/web/__init__.py
  </files>
  <read_first>
    job_finder/web/blueprints/profile.py
    job_finder/web/__init__.py
    .planning/phases/22-module-splits/22-RESEARCH.md (SPLIT-05 section)
  </read_first>
  <action>
    **Create `job_finder/web/blueprints/resume_review.py`** (~310 LOC):
    - Blueprint definition: `resume_review_bp = Blueprint("resume_review", __name__, url_prefix="/profile")`
    - Move `CONFLICT_SCHEMA` constant from `profile.py` (co-locate with the routes that use it)
    - Move from `profile.py` (change decorator from `@profile_bp.route` to `@resume_review_bp.route`):
      - `upload_pdf()` (lines 290-358)
      - `_compare_conflicts()` (lines 359-416) — helper, not a route
      - `conflict_review()` (lines 419-456)
      - `save_conflicts()` (lines 459-548)
      - `extract_style()` (lines 550-596)
      - `save_style_guide_route()` (lines 599-619)
    - **Critical url_for fix inside `upload_pdf()`**: Change `url_for("profile.conflict_review", ...)` to `url_for("resume_review.conflict_review", ...)` since `conflict_review` is now in `resume_review_bp`
    - Copy all imports needed by moved functions
    - Add module docstring: "Resume review blueprint -- PDF upload, conflict review, style extraction routes."
    - Add `logger = logging.getLogger(__name__)`

    **Create `job_finder/web/blueprints/profile_recommendations.py`** (~270 LOC):
    - Blueprint definition: `profile_recs_bp = Blueprint("profile_recommendations", __name__, url_prefix="/profile")`
    - Move `RECOMMENDATION_SCHEMA` constant from `profile.py` (co-locate with recommendation routes)
    - Move `_SAFE_ACTION_TYPES` and `_SAFE_EDITABLE_FIELDS` constants if used exclusively by moved routes (check usage first)
    - Move from `profile.py` (change decorator from `@profile_bp.route` to `@profile_recs_bp.route`):
      - `recommendation()` (lines 676-763)
      - `recommendations_all()` (lines 766-846)
      - `apply_fix()` (lines 847-911)
    - Import `_get_all_skills` from `profile.py` if `apply_fix` needs it:
      ```python
      from job_finder.web.blueprints.profile import _get_all_skills
      ```
    - Copy all imports needed by moved functions
    - Add module docstring: "Profile recommendations blueprint -- AI-powered profile improvement suggestions."
    - Add `logger = logging.getLogger(__name__)`

    **Update `job_finder/web/blueprints/profile.py`**:
    - Remove all moved routes and helpers
    - Remove `CONFLICT_SCHEMA` (moved to `resume_review.py`) and `RECOMMENDATION_SCHEMA` (moved to `profile_recommendations.py`)
    - Remove `_SAFE_ACTION_TYPES`/`_SAFE_EDITABLE_FIELDS` if exclusively used by moved routes
    - Remove imports only used by moved functions
    - Keep: `_get_all_skills()`, `_load_profile_page_extras()`, `index()`, `save()`, `import_markdown()`, `reorder_positions()`, `reorder_skills()`

    **Update `job_finder/web/__init__.py`**:
    - Note: `__init__.py` was modified by Plans 22-03 and 22-05. Read the current state first.
    - Add imports and registrations after existing `profile_bp` registration:
      ```python
      from job_finder.web.blueprints.resume_review import resume_review_bp
      from job_finder.web.blueprints.profile_recommendations import profile_recs_bp
      app.register_blueprint(resume_review_bp)
      app.register_blueprint(profile_recs_bp)
      ```

    **Grep check before finalizing**: Search all `.py` and `.html` files for `url_for("profile.` to find any references to moved routes that need updating. Key ones per research:
    - `url_for("profile.conflict_review")` in `upload_pdf()` -> already moving to resume_review.py, fix the reference
    - Other `url_for("profile.index")` references stay in profile.py routes — no change needed
  </action>
  <verify>
    <automated>cd /c/Users/senki/repos/job-cannon && uv run python -c "from job_finder.web.blueprints.resume_review import resume_review_bp; from job_finder.web.blueprints.profile_recommendations import profile_recs_bp; from job_finder.web.blueprints.profile import profile_bp; print('All blueprints importable')"</automated>
  </verify>
  <acceptance_criteria>
    - `job_finder/web/blueprints/resume_review.py` exists with `resume_review_bp` Blueprint using `url_prefix="/profile"`
    - `job_finder/web/blueprints/profile_recommendations.py` exists with `profile_recs_bp` Blueprint using `url_prefix="/profile"`
    - Both new blueprints are registered in `create_app()` in `__init__.py`
    - `profile.py` only contains profile management routes (index, save, import, reorder)
    - `upload_pdf()` uses `url_for("resume_review.conflict_review")` not `url_for("profile.conflict_review")`
    - No `ImportError` when importing any of the three blueprints
  </acceptance_criteria>
  <done>Three blueprints exist, all registered in create_app(), url_for references correct, no import errors</done>
</task>

<task type="auto">
  <name>Task 2: Update test imports and monkeypatch targets for SPLIT-05</name>
  <files>
    tests/test_profile.py
  </files>
  <read_first>
    tests/test_profile.py
    .planning/phases/22-module-splits/22-RESEARCH.md (SPLIT-05 Test Updates section)
  </read_first>
  <action>
    **Update `tests/test_profile.py`**:
    - Find monkeypatch targets for `_compare_conflicts` (lines 828-830, 1146 per research):
      - Old: `monkeypatch.setattr(profile_mod, "_compare_conflicts", ...)`
      - New: Import `job_finder.web.blueprints.resume_review as resume_review_mod` and patch: `monkeypatch.setattr(resume_review_mod, "_compare_conflicts", ...)`
    - Check for any direct imports of moved functions from `profile_mod`:
      - `upload_pdf`, `conflict_review`, `save_conflicts`, `extract_style`, `save_style_guide_route`, `recommendation`, `recommendations_all`, `apply_fix`
      - Update to import from the correct new module
    - Check for monkeypatch targets referencing `CONFLICT_SCHEMA` or `RECOMMENDATION_SCHEMA` from `profile_mod` — update to new module
    - Routes tested via Flask test client (e.g., `client.post("/profile/upload-pdf")`) should NOT need changes since url_prefix is the same
    - Keep `profile_mod` references for things that stay: `_PROFILE_PATH`, `_get_all_skills`, `_load_profile_page_extras`, `index`, `save`, etc.

    Run full test suite after changes.
  </action>
  <verify>
    <automated>cd /c/Users/senki/repos/job-cannon && uv run pytest tests/test_profile.py -x --tb=short -q</automated>
  </verify>
  <acceptance_criteria>
    - No monkeypatch targets reference moved functions at old `profile` module location
    - `_compare_conflicts` patches target `resume_review` module
    - All profile tests pass
    - Full test suite: `uv run pytest tests/ -x --tb=short -q` passes
  </acceptance_criteria>
  <done>All test imports and monkeypatch targets updated; full test suite green</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/ -x --tb=short -q` — full suite passes with no regressions
- `wc -l job_finder/web/blueprints/profile.py job_finder/web/blueprints/resume_review.py job_finder/web/blueprints/profile_recommendations.py` — combined LOC does not exceed pre-split 913
</verification>

<success_criteria>
- Three registered blueprints with clear responsibility boundaries
- All profile, resume review, and recommendation routes respond correctly
- url_for references correctly target new blueprint names
- All tests pass with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/22-module-splits/22-06-SUMMARY.md`
</output>
