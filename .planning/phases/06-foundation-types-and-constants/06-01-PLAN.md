---
phase: 06-foundation-types-and-constants
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - job_finder/json_utils.py
  - job_finder/db.py
  - job_finder/web/db_helpers.py
  - job_finder/main.py
  - job_finder/utils.py
  - job_finder/output/__init__.py
autonomous: true
requirements: [CLN-01]
must_haves:
  truths:
    - "safe_json_load is importable from job_finder.json_utils"
    - "safe_json_load is still importable from job_finder.web.db_helpers (re-export)"
    - "job_finder.utils module no longer exists"
    - "job_finder.output package no longer exists"
    - "All 266 existing tests pass"
  artifacts:
    - path: "job_finder/json_utils.py"
      provides: "safe_json_load function"
      exports: ["safe_json_load"]
    - path: "job_finder/db.py"
      provides: "Updated import from json_utils"
      contains: "from job_finder.json_utils import safe_json_load"
    - path: "job_finder/web/db_helpers.py"
      provides: "Updated re-export from json_utils"
      contains: "from job_finder.json_utils import safe_json_load"
    - path: "job_finder/main.py"
      provides: "Updated import from json_utils"
      contains: "from job_finder.json_utils import safe_json_load"
  key_links:
    - from: "job_finder/db.py"
      to: "job_finder/json_utils.py"
      via: "import"
      pattern: "from job_finder\\.json_utils import safe_json_load"
    - from: "job_finder/web/db_helpers.py"
      to: "job_finder/json_utils.py"
      via: "import re-export"
      pattern: "from job_finder\\.json_utils import safe_json_load"
    - from: "tests/test_db_helpers.py"
      to: "job_finder/web/db_helpers.py"
      via: "import"
      pattern: "from job_finder\\.web\\.db_helpers import safe_json_load"
---

<objective>
Create json_utils.py module, migrate all safe_json_load imports, and delete superseded dead code (utils.py, output/).

Purpose: CLN-01 requires removal of dead modules. json_utils.py replaces utils.py with the same function. All three callers (db.py, db_helpers.py, main.py) must be updated before utils.py can be safely deleted.

Output: json_utils.py exists, all imports updated, utils.py and output/ deleted, tests pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create json_utils.py and update all imports</name>
  <files>job_finder/json_utils.py, job_finder/db.py, job_finder/web/db_helpers.py, job_finder/main.py</files>
  <read_first>
    <other-repo>/job_finder/json_utils.py
    <repo-root>/job_finder/utils.py
    <repo-root>/job_finder/db.py
    <repo-root>/job_finder/web/db_helpers.py
    <repo-root>/job_finder/main.py
  </read_first>
  <action>
1. Create `job_finder/json_utils.py` by copying the content from `<other-repo>\job_finder\json_utils.py`. The file contains:
   - Module docstring: `"""JSON deserialization utilities shared across persistence and web layers."""`
   - Imports: `json`, `logging`, `typing.Any`
   - Logger: `logger = logging.getLogger(__name__)`
   - Function: `safe_json_load(value: str | None, default: Any = None) -> Any`
   - Exception handling catches `json.JSONDecodeError`, `TypeError`, `ValueError`

2. In `job_finder/db.py` line 10, change:
   `from job_finder.utils import safe_json_load`
   to:
   `from job_finder.json_utils import safe_json_load`

3. In `job_finder/web/db_helpers.py` line 16, change:
   `from job_finder.utils import safe_json_load  # noqa: F401 -- re-exported for backward compatibility`
   to:
   `from job_finder.json_utils import safe_json_load  # noqa: F401 -- re-exported for backward compatibility`
   This re-export MUST be preserved because `tests/test_db_helpers.py` imports `safe_json_load` from `job_finder.web.db_helpers`.

4. In `job_finder/main.py` line 32, change:
   `from job_finder.utils import safe_json_load`
   to:
   `from job_finder.json_utils import safe_json_load`
   (main.py is dead code deleted in Phase 10, but the import must work now so Python doesn't fail on import.)
  </action>
  <verify>
    <automated>uv run python -c "from job_finder.json_utils import safe_json_load; print(safe_json_load('{\"a\":1}'))" && uv run python -c "from job_finder.web.db_helpers import safe_json_load; print('re-export ok')" && uv run python -c "from job_finder.db import get_job; print('db import ok')"</automated>
  </verify>
  <acceptance_criteria>
    - File `job_finder/json_utils.py` exists and contains `def safe_json_load(`
    - `job_finder/db.py` contains the string `from job_finder.json_utils import safe_json_load`
    - `job_finder/web/db_helpers.py` contains the string `from job_finder.json_utils import safe_json_load`
    - `job_finder/main.py` contains the string `from job_finder.json_utils import safe_json_load`
    - `uv run python -c "from job_finder.json_utils import safe_json_load"` exits 0
    - `uv run python -c "from job_finder.web.db_helpers import safe_json_load"` exits 0
  </acceptance_criteria>
  <done>json_utils.py created, all three callers updated, both direct and re-exported imports work</done>
</task>

<task type="auto">
  <name>Task 2: Delete utils.py and output/ package</name>
  <files>job_finder/utils.py, job_finder/output/__init__.py</files>
  <read_first>
    <repo-root>/job_finder/utils.py
    <repo-root>/job_finder/output/__init__.py
  </read_first>
  <action>
1. Delete `job_finder/utils.py` using `git rm job_finder/utils.py`.
2. Delete `job_finder/output/__init__.py` and the `job_finder/output/` directory using `git rm -r job_finder/output/`.
3. Verify no remaining imports reference `job_finder.utils` (excluding docs/ which are documentation-only markdown files).
  </action>
  <verify>
    <automated>uv run pytest tests/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - File `job_finder/utils.py` does not exist
    - Directory `job_finder/output/` does not exist
    - `grep -r "from job_finder.utils import" job_finder/` returns no results (only docs/ may still reference it)
    - `uv run pytest tests/ -x -q` passes all 266 tests
  </acceptance_criteria>
  <done>Dead code removed. utils.py and output/ no longer exist. All tests pass.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/ -x -q` passes all 266 tests
- `uv run python -c "from job_finder.json_utils import safe_json_load; print('ok')"` succeeds
- `python -c "import job_finder.utils"` fails with ModuleNotFoundError (confirming deletion)
- `python -c "import job_finder.output"` fails with ModuleNotFoundError (confirming deletion)
</verification>

<success_criteria>
All 266 tests pass, json_utils.py importable, utils.py and output/ deleted, no broken imports in job_finder/ package.
</success_criteria>

<output>
After completion, create `.planning/phases/06-foundation-types-and-constants/06-01-SUMMARY.md`
</output>
