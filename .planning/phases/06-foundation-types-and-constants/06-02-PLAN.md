---
phase: 06-foundation-types-and-constants
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - job_finder/web/scoring_types.py
  - job_finder/web/blueprints/__init__.py
  - job_finder/config.py
  - job_finder/web/__init__.py
autonomous: true
requirements: [SAFE-06, CLN-01]
must_haves:
  truths:
    - "scoring_types.py is importable from job_finder.web.scoring_types"
    - "PIPELINE_STATUSES is a tuple (not a list)"
    - "VALID_PIPELINE_STATUSES is a frozenset of PIPELINE_STATUSES"
    - "DEFAULT_BORDERLINE_HIGH constant equals 54"
    - "Flask app generates a random secret key on each startup via secrets.token_hex(32)"
    - "All 266 existing tests pass"
  artifacts:
    - path: "job_finder/web/scoring_types.py"
      provides: "JobRow TypedDict, ScoringResult NamedTuple, format_salary_range function"
      exports: ["JobRow", "ScoringResult", "format_salary_range"]
    - path: "job_finder/web/blueprints/__init__.py"
      provides: "PIPELINE_STATUSES tuple and VALID_PIPELINE_STATUSES frozenset"
      contains: "VALID_PIPELINE_STATUSES = frozenset(PIPELINE_STATUSES)"
    - path: "job_finder/config.py"
      provides: "DEFAULT_BORDERLINE_HIGH = 54 constant"
      contains: "DEFAULT_BORDERLINE_HIGH = 54"
    - path: "job_finder/web/__init__.py"
      provides: "Secret key via secrets.token_hex(32)"
      contains: "secrets.token_hex(32)"
  key_links:
    - from: "job_finder/web/blueprints/__init__.py"
      to: "PIPELINE_STATUSES"
      via: "tuple definition"
      pattern: "PIPELINE_STATUSES = \\("
    - from: "job_finder/web/__init__.py"
      to: "secrets module"
      via: "import and usage"
      pattern: "secrets\\.token_hex"
---

<objective>
Create scoring_types.py, harden constants (tuple + frozenset), add DEFAULT_BORDERLINE_HIGH, and fix the Flask secret key to use secrets.token_hex(32).

Purpose: SAFE-06 requires a non-hardcoded secret key. CLN-01 requires new utility modules. The constants and types created here are consumed by later phases (Phase 7 scoring orchestrator, Phase 8 scorers, Phase 10 status validation).

Output: scoring_types.py exists, PIPELINE_STATUSES is a tuple, VALID_PIPELINE_STATUSES frozenset exists, DEFAULT_BORDERLINE_HIGH=54 exists in config.py, Flask secret key is randomized.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scoring_types.py and update constants</name>
  <files>job_finder/web/scoring_types.py, job_finder/web/blueprints/__init__.py, job_finder/config.py</files>
  <read_first>
    <other-repo>/job_finder/web/scoring_types.py
    <repo-root>/job_finder/web/blueprints/__init__.py
    <repo-root>/job_finder/config.py
  </read_first>
  <action>
1. Create `job_finder/web/scoring_types.py` by copying from `<other-repo>\job_finder\web\scoring_types.py`. The file contains:
   - Module docstring about shared types for the scoring pipeline
   - Imports: `Literal`, `NamedTuple`, `TypedDict` from `typing`
   - `class JobRow(TypedDict, total=False)` with fields: dedup_key, title, company, location, description, jd_full, salary_min, salary_max, source_urls, comp_data_json, haiku_score, sonnet_score, fit_analysis, enrichment_tier, company_id, status
   - `def format_salary_range(salary_min: int | None, salary_max: int | None) -> str` returning formatted salary strings
   - `class ScoringResult(NamedTuple)` with fields: `data: dict | None`, `status: Literal["success", "budget_exceeded", "error", "skipped"]`

2. In `job_finder/web/blueprints/__init__.py`, change PIPELINE_STATUSES from a list to a tuple (replace `[` with `(` and `]` with `)` on lines 5-17) and add `VALID_PIPELINE_STATUSES = frozenset(PIPELINE_STATUSES)` on the line after the tuple definition (before the blank line before `def trigger_interview_prep_if_applied`).

   The result should be:
   ```python
   PIPELINE_STATUSES = (
       "discovered",
       "reviewing",
       "applied",
       "phone_screen",
       "technical",
       "onsite",
       "offer",
       "accepted",
       "archived",
       "rejected",
       "withdrawn",
   )

   VALID_PIPELINE_STATUSES = frozenset(PIPELINE_STATUSES)
   ```

3. In `job_finder/config.py`, add `DEFAULT_BORDERLINE_HIGH = 54` in the "Scoring defaults" section. Insert it after `DEFAULT_HAIKU_THRESHOLD = 42` (after line 19, before `DEFAULT_MONTHLY_BUDGET_USD = 25.0`).
  </action>
  <verify>
    <automated>uv run python -c "from job_finder.web.scoring_types import JobRow, ScoringResult, format_salary_range; print(format_salary_range(80000, 120000))" && uv run python -c "from job_finder.web.blueprints import PIPELINE_STATUSES, VALID_PIPELINE_STATUSES; assert isinstance(PIPELINE_STATUSES, tuple), 'not tuple'; assert isinstance(VALID_PIPELINE_STATUSES, frozenset), 'not frozenset'; print('ok')" && uv run python -c "from job_finder.config import DEFAULT_BORDERLINE_HIGH; assert DEFAULT_BORDERLINE_HIGH == 54; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - File `job_finder/web/scoring_types.py` exists and contains `class JobRow(TypedDict`, `class ScoringResult(NamedTuple)`, and `def format_salary_range(`
    - `job_finder/web/blueprints/__init__.py` contains `PIPELINE_STATUSES = (` (tuple, not list)
    - `job_finder/web/blueprints/__init__.py` contains `VALID_PIPELINE_STATUSES = frozenset(PIPELINE_STATUSES)`
    - `job_finder/config.py` contains `DEFAULT_BORDERLINE_HIGH = 54`
    - `uv run python -c "from job_finder.web.scoring_types import JobRow, ScoringResult, format_salary_range"` exits 0
    - `uv run python -c "from job_finder.web.blueprints import VALID_PIPELINE_STATUSES"` exits 0
  </acceptance_criteria>
  <done>scoring_types.py created with JobRow, ScoringResult, format_salary_range. PIPELINE_STATUSES is a tuple. VALID_PIPELINE_STATUSES frozenset exists. DEFAULT_BORDERLINE_HIGH = 54 in config.py.</done>
</task>

<task type="auto">
  <name>Task 2: Fix Flask secret key to use secrets.token_hex(32)</name>
  <files>job_finder/web/__init__.py</files>
  <read_first>
    <repo-root>/job_finder/web/__init__.py
  </read_first>
  <action>
1. Add `import secrets` to the imports at the top of `job_finder/web/__init__.py` (after the existing `import os` on line 13).

2. Replace line 95:
   `app.secret_key = os.environ.get("FLASK_SECRET_KEY", "dev-secret-key-change-in-production")`
   with:
   `app.secret_key = os.environ.get("FLASK_SECRET_KEY") or secrets.token_hex(32)`

   This preserves the ability to set a fixed key via env var (for session persistence across restarts if desired) but generates a cryptographically random 64-character hex string as the default instead of a hardcoded string.
  </action>
  <verify>
    <automated>uv run python -c "import os; os.environ.pop('FLASK_SECRET_KEY', None); from job_finder.web import create_app; app = create_app(config={'db': {'path': ':memory:'}, 'profile': {}, 'sources': {}, 'scoring': {}, 'TESTING': True}); assert app.secret_key != 'dev-secret-key-change-in-production', f'still hardcoded: {app.secret_key}'; assert len(app.secret_key) == 64, f'wrong length: {len(app.secret_key)}'; print('secret key ok:', app.secret_key[:8] + '...')"</automated>
  </verify>
  <acceptance_criteria>
    - `job_finder/web/__init__.py` contains `import secrets`
    - `job_finder/web/__init__.py` contains `secrets.token_hex(32)`
    - `job_finder/web/__init__.py` does NOT contain the string `dev-secret-key-change-in-production`
    - Creating a Flask app without FLASK_SECRET_KEY env var produces a 64-character hex secret key
    - `uv run pytest tests/ -x -q` passes all 266 tests
  </acceptance_criteria>
  <done>Flask secret key uses secrets.token_hex(32) instead of hardcoded string. SAFE-06 addressed.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/ -x -q` passes all 266 tests
- `uv run python -c "from job_finder.web.scoring_types import JobRow, ScoringResult, format_salary_range"` succeeds
- `uv run python -c "from job_finder.web.blueprints import PIPELINE_STATUSES; assert isinstance(PIPELINE_STATUSES, tuple)"` succeeds
- `uv run python -c "from job_finder.web.blueprints import VALID_PIPELINE_STATUSES; assert isinstance(VALID_PIPELINE_STATUSES, frozenset)"` succeeds
- `uv run python -c "from job_finder.config import DEFAULT_BORDERLINE_HIGH; assert DEFAULT_BORDERLINE_HIGH == 54"` succeeds
- Flask app secret key is not 'dev-secret-key-change-in-production'
</verification>

<success_criteria>
All 266 tests pass. scoring_types.py importable. PIPELINE_STATUSES is tuple, VALID_PIPELINE_STATUSES is frozenset. DEFAULT_BORDERLINE_HIGH=54 exists. Flask secret key randomized per SAFE-06.
</success_criteria>

<output>
After completion, create `.planning/phases/06-foundation-types-and-constants/06-02-SUMMARY.md`
</output>
