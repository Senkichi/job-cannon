---
phase: 13-planning-doc-corrections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/PROJECT.md
  - .planning/STATE.md
  - CLAUDE.md
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02
  - DOCS-03
  - DOCS-04

must_haves:
  truths:
    - "CLAUDE.md says 'Job Cannon' not 'Job Finder' in project overview"
    - "CLAUDE.md test count says 1359 not 266"
    - "CLAUDE.md Phase 4/5 status says Operational not 'Not started'"
    - "PROJECT.md Context section has no 'deferred' references for Phase 4/5"
    - "PROJECT.md includes Phase 4/5 in Validated requirements"
    - "STATE.md status reflects migration in progress"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Corrected project instructions"
      contains: "Job Cannon is a personal"
    - path: ".planning/PROJECT.md"
      provides: "Corrected project overview"
      contains: "All features operational"
    - path: ".planning/STATE.md"
      provides: "Corrected project state"
      contains: "all features operational"
  key_links:
    - from: "CLAUDE.md"
      to: ".planning/ROADMAP.md"
      via: "Phase 4/5 status alignment"
      pattern: "Operational.*inherited"
    - from: ".planning/PROJECT.md"
      to: ".planning/ROADMAP.md"
      via: "Phase 4/5 validated requirements"
      pattern: "inherited from job-finder"
---

<objective>
Correct the three main project documentation files (CLAUDE.md, PROJECT.md, STATE.md) to accurately reflect Phase 4/5 as operational, fix stale stats, and remove "deferred" references.

Purpose: Planning docs currently say Phase 4/5 are "Not started" or "deferred" and show outdated test counts (266). These features were inherited from job-finder and are operational. Docs must match reality.
Output: Three corrected documentation files with accurate status, stats, and no stale references.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@docs/superpowers/plans/2026-03-24-migration-and-stabilization.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CLAUDE.md — project name, test counts, phase status</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (current state — 180 lines)
    - .planning/ROADMAP.md (source of truth for Phase 4/5 status)
    - docs/superpowers/plans/2026-03-24-migration-and-stabilization.md (Task 4 reference edits)
  </read_first>
  <action>
Make 6 surgical edits to CLAUDE.md using the Edit tool. Each edit is an exact string replacement:

**Edit 1 — Fix project name in overview (line 5, DOCS-04):**
Old string:
```
Job Finder is a personal job search command center. Flask web app (localhost:5000) that aggregates jobs from Gmail alerts (LinkedIn, Glassdoor, ZipRecruiter) and SerpAPI, scores them with a two-tier Claude AI pipeline (Haiku fast filter → Sonnet deep evaluation), tracks application pipeline status, and will generate tailored resumes via Google Docs.
```
New string:
```
Job Cannon is a personal job search command center. Flask web app (localhost:5000) that aggregates jobs from Gmail alerts (LinkedIn, Glassdoor, ZipRecruiter) and SerpAPI, scores them with a two-tier Claude AI pipeline (Haiku fast filter → Sonnet deep evaluation), tracks application pipeline status, and generates tailored resumes via Google Docs.
```
(Two changes: "Job Finder" → "Job Cannon", "will generate" → "generates")

**Edit 2 — Remove "(Phase 4)" from APIs line (line 16, DOCS-01):**
Old string:
```
- **APIs**: Gmail API v1 (OAuth 2.0), Google Drive/Docs API (Phase 4), SerpAPI (optional)
```
New string:
```
- **APIs**: Gmail API v1 (OAuth 2.0), Google Drive/Docs API, SerpAPI (optional)
```

**Edit 3 — Update test count in Key Commands (line 25, DOCS-02):**
Old string:
```
uv run pytest tests/                              # All tests (266 passing)
```
New string:
```
uv run pytest tests/                              # All tests (1359 passing)
```

**Edit 4 — Update test file count in Project Structure (line 62, DOCS-02):**
Old string:
```
└── test_*.py                    # 11 test files
```
New string:
```
└── test_*.py                    # 41 test files
```

**Edit 5 — Update Planning Documentation description (lines 97-98, DOCS-01):**
Old string:
```
- `.planning/ROADMAP.md` — 5-phase milestone with success criteria
- `.planning/STATE.md` — Current state, 100+ architectural decisions, pending todos
```
New string:
```
- `.planning/ROADMAP.md` — All phases complete, milestone history
- `.planning/STATE.md` — Current state, 100+ architectural decisions
```

**Edit 6 — Update Current Status section (lines 101-107, DOCS-01):**
Old string:
```
## Current Status

- **Phase 1 (Foundation)**: Complete — 11/11 plans, 36/36 must-haves verified
- **Phase 2 (AI Scoring)**: Complete — 5/5 plans
- **Phase 3 (Pipeline Automation)**: Complete — 2/2 plans
- **Phase 4 (Resume Generation)**: Not started (context gathered)
- **Phase 5 (Intelligence)**: Not started
```
New string:
```
## Current Status

- **Phase 1 (Foundation)**: Complete — 11/11 plans, 36/36 must-haves verified
- **Phase 2 (AI Scoring)**: Complete — 5/5 plans
- **Phase 3 (Pipeline Automation)**: Complete — 2/2 plans
- **Phase 4 (Resume Generation)**: Operational — inherited from job-finder (resume generator, Drive upload, DOCX formatter, feedback, validation)
- **Phase 5 (Intelligence)**: Operational — inherited from job-finder (interview prep, rejection analysis, notifications). Semantic similarity/clustering dropped.
```
  </action>
  <verify>
    <automated>cd <repo-root> && grep -c "Job Cannon is a personal" CLAUDE.md && grep -c "1359 passing" CLAUDE.md && grep -c "41 test files" CLAUDE.md && grep -c "Not started" CLAUDE.md && grep -c "Phase 4)" CLAUDE.md</automated>
    Expected output: 1, 1, 1, 0, 0 (first three present, last two absent)
  </verify>
  <acceptance_criteria>
    - `grep "Job Cannon is a personal" CLAUDE.md` returns exactly 1 match
    - `grep "Job Finder" CLAUDE.md` returns 0 matches (the old name is gone from overview)
    - `grep "1359 passing" CLAUDE.md` returns exactly 1 match
    - `grep "41 test files" CLAUDE.md` returns exactly 1 match
    - `grep "Not started" CLAUDE.md` returns 0 matches
    - `grep "Phase 4)" CLAUDE.md` returns 0 matches
    - `grep "Operational.*inherited" CLAUDE.md` returns 2 matches (Phase 4 and Phase 5 lines)
    - `grep "will generate" CLAUDE.md` returns 0 matches
    - `grep "generates tailored" CLAUDE.md` returns 1 match
    - `grep "All phases complete, milestone history" CLAUDE.md` returns 1 match
  </acceptance_criteria>
  <done>CLAUDE.md says "Job Cannon", shows 1359 tests / 41 test files, Phase 4/5 are "Operational — inherited from job-finder", no "(Phase 4)" annotation on APIs line, planning docs description updated</done>
</task>

<task type="auto">
  <name>Task 2: Update PROJECT.md — validated requirements, current state, context, constraints</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md (current state — 100 lines)
    - .planning/ROADMAP.md (source of truth for Phase 4/5 status)
    - docs/superpowers/plans/2026-03-24-migration-and-stabilization.md (Task 2 reference edits)
  </read_first>
  <action>
Make 4 surgical edits to .planning/PROJECT.md using the Edit tool:

**Edit 1 — Add Phase 4/5 to Validated requirements (after line 27, DOCS-01):**
Old string:
```
- ✓ Dead code removal: main.py, utils.py, output/ (scoring/ retained — active) — v1.1

### Active
```
New string:
```
- ✓ Dead code removal: main.py, utils.py, output/ (scoring/ retained — active) — v1.1
- Tailored resume generation with Google Drive upload, multi-variant synthesis, feedback + style guide (inherited from job-finder)
- Interview prep auto-generation with Opus, rejection pattern analysis, Windows toast notifications (inherited from job-finder)

### Active
```

**Edit 2 — Update Current State section (line 51, DOCS-02):**
Old string:
```
v1.1 shipped (2026-03-24). All 27 requirements satisfied. 1370 tests pass. job-finder is retired; job-cannon is the single source of truth.
```
New string:
```
v1.1 shipped (2026-03-24). All features operational. 1359 tests pass across 41 test files. job-finder is retired; job-cannon is the single source of truth.
```

**Edit 3 — Update Context section (lines 57-60, DOCS-03):**
Old string:
```
- job-cannon is the single source of truth (job-finder retired after v1.1)
- 52,780 LOC Python across job_finder/ and tests/
- 1370 tests, all passing
- Next candidates: Resume generation (Phase 4 deferred), Intelligence features (Phase 5 deferred)
```
New string:
```
- job-cannon is the single source of truth (job-finder retired after v1.1)
- 52,381 LOC Python across job_finder/ and tests/
- 1359 tests, all passing
- All features operational — resume generation, interview prep, rejection analysis, scoring, pipeline tracking
```

**Edit 4 — Update Constraints section (lines 66-67, DOCS-03):**
Old string:
```
- **Source repo**: job-finder at `<other-repo>` must remain readable during port
- **Test preservation**: 3 cannon-only test files must survive the port
```
New string:
```
- **Source repo**: job-finder at `<other-repo>` (retired, read-only reference)
```
  </action>
  <verify>
    <automated>cd <repo-root> && grep -c "deferred" .planning/PROJECT.md && grep -c "inherited from job-finder" .planning/PROJECT.md && grep -c "1359 tests" .planning/PROJECT.md && grep -c "must remain readable" .planning/PROJECT.md</automated>
    Expected output: 0, 2, 1, 0 (no deferred, two inherited lines, one test count, no stale constraint)
  </verify>
  <acceptance_criteria>
    - `grep "deferred" .planning/PROJECT.md` returns 0 matches
    - `grep "inherited from job-finder" .planning/PROJECT.md` returns exactly 2 matches (one for resume gen, one for interview prep)
    - `grep "1359 tests" .planning/PROJECT.md` returns exactly 1 match
    - `grep "41 test files" .planning/PROJECT.md` returns exactly 1 match
    - `grep "must remain readable during port" .planning/PROJECT.md` returns 0 matches
    - `grep "Test preservation" .planning/PROJECT.md` returns 0 matches
    - `grep "retired, read-only reference" .planning/PROJECT.md` returns 1 match
    - `grep "52,381 LOC" .planning/PROJECT.md` returns 1 match
    - `grep "All features operational" .planning/PROJECT.md` returns 1 match
  </acceptance_criteria>
  <done>PROJECT.md has Phase 4/5 in Validated requirements, current state shows 1359 tests, context has no "deferred" references, constraints updated for retired job-finder</done>
</task>

<task type="auto">
  <name>Task 3: Update STATE.md — align status with v1.2 migration progress</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (current state — 68 lines, ALREADY partially updated for v1.2)
    - docs/superpowers/plans/2026-03-24-migration-and-stabilization.md (Task 3 reference)
  </read_first>
  <action>
STATE.md was already updated when v1.2 roadmap was created. The implementation plan's Task 3 assumed old v1.1 content that no longer exists. Make these 2 surgical adjustments to align with the current actual content:

**Edit 1 — Update frontmatter status (line 5, DOCS-01):**
Old string:
```
status: Roadmap defined, ready to plan Phase 13
```
New string:
```
status: all features operational, migration and stabilization in progress
```

**Edit 2 — Update Current focus line (line 30, DOCS-01):**
Old string:
```
**Current focus:** v1.2 — Phase 13: Planning Doc Corrections
```
New string:
```
**Current focus:** Migration from job-finder + planning doc updates
```

NOTE: Lines 18-19 (Phase/Plan), line 21 (Last activity), and line 67 (Last session) are fine as-is since they already reflect v1.2 context. Do NOT change them — they correctly track position within the v1.2 milestone.
  </action>
  <verify>
    <automated>cd <repo-root> && grep -c "all features operational" .planning/STATE.md && grep -c "Migration from job-finder" .planning/STATE.md && grep -c "v1.1 milestone complete" .planning/STATE.md</automated>
    Expected output: 1, 1, 0
  </verify>
  <acceptance_criteria>
    - `grep "all features operational" .planning/STATE.md` returns exactly 1 match
    - `grep "Migration from job-finder" .planning/STATE.md` returns exactly 1 match
    - `grep "v1.1 milestone complete" .planning/STATE.md` returns 0 matches
    - `grep "Roadmap defined, ready to plan" .planning/STATE.md` returns 0 matches
  </acceptance_criteria>
  <done>STATE.md frontmatter status says "all features operational, migration and stabilization in progress", current focus says "Migration from job-finder + planning doc updates"</done>
</task>

</tasks>

<verification>
After all 3 tasks complete, run:
```bash
grep -c "Job Cannon is a personal" CLAUDE.md
grep -c "Not started" CLAUDE.md
grep -rn "deferred" .planning/PROJECT.md CLAUDE.md .planning/STATE.md
grep -rn "Phase 4)" CLAUDE.md .planning/codebase/STACK.md .planning/codebase/INTEGRATIONS.md
```
Expected: "Job Cannon" present, zero "Not started", zero "deferred" in main docs, zero "Phase 4)" annotations.
</verification>

<success_criteria>
- CLAUDE.md project overview says "Job Cannon" (not "Job Finder") — DOCS-04
- CLAUDE.md test count says 1359 (not 266) and 41 test files (not 11) — DOCS-02
- CLAUDE.md Phase 4/5 status says "Operational" (not "Not started") — DOCS-01
- CLAUDE.md APIs line has no "(Phase 4)" annotation — DOCS-01
- PROJECT.md Context section has no "deferred" Phase 4/5 references — DOCS-03
- PROJECT.md Validated requirements include Phase 4/5 features — DOCS-01
- PROJECT.md Current State shows 1359 tests — DOCS-02
- STATE.md status reflects operational state with migration in progress — DOCS-01
</success_criteria>

<output>
After completion, create `.planning/phases/13-planning-doc-corrections/13-01-SUMMARY.md`
</output>
