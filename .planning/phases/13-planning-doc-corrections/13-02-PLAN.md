---
phase: 13-planning-doc-corrections
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/codebase/STACK.md
  - .planning/codebase/INTEGRATIONS.md
autonomous: true
requirements:
  - DOCS-01
  - DOCS-03

must_haves:
  truths:
    - "STACK.md has no '(Phase 5)' or '(Phase 4+)' annotations"
    - "INTEGRATIONS.md has no '(Phase 4+)', '(Phase 4)', or '(Phase 4, future)' annotations"
    - "Zero grep matches for phase annotations across all planning docs and CLAUDE.md"
  artifacts:
    - path: ".planning/codebase/STACK.md"
      provides: "Clean technology stack documentation"
      contains: "Notifications:"
    - path: ".planning/codebase/INTEGRATIONS.md"
      provides: "Clean integration documentation"
      contains: "Upload/manage Drive files"
  key_links:
    - from: ".planning/codebase/STACK.md"
      to: ".planning/ROADMAP.md"
      via: "Phase annotations removed to match operational status"
      pattern: "Notifications:"
    - from: ".planning/codebase/INTEGRATIONS.md"
      to: ".planning/ROADMAP.md"
      via: "Phase annotations removed to match operational status"
      pattern: "drive.folder_id.*Google Drive folder"
---

<objective>
Remove stale phase annotations from codebase documentation (STACK.md and INTEGRATIONS.md) and run a final verification sweep across all planning docs.

Purpose: STACK.md and INTEGRATIONS.md still have "(Phase 5)", "(Phase 4+)", "(Phase 4)", and "(Phase 4, future)" annotations that imply these features are upcoming. They are operational. Remove the annotations.
Output: Clean codebase docs with no phase annotations, plus verification that all planning docs are consistent.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/codebase/STACK.md
@.planning/codebase/INTEGRATIONS.md
@.planning/ROADMAP.md
@docs/superpowers/plans/2026-03-24-migration-and-stabilization.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove phase annotations from STACK.md and INTEGRATIONS.md</name>
  <files>.planning/codebase/STACK.md, .planning/codebase/INTEGRATIONS.md</files>
  <read_first>
    - .planning/codebase/STACK.md (current state — 143 lines)
    - .planning/codebase/INTEGRATIONS.md (current state — 187 lines)
    - docs/superpowers/plans/2026-03-24-migration-and-stabilization.md (Task 5 reference edits)
  </read_first>
  <action>
Make 6 surgical edits using the Edit tool — 2 in STACK.md, 4 in INTEGRATIONS.md:

**STACK.md Edit 1 — Remove "(Phase 5)" from Notifications header (line 66, DOCS-01):**
Old string:
```
**Notifications (Phase 5):**
```
New string:
```
**Notifications:**
```

**STACK.md Edit 2 — Remove "(Phase 4+)" from Drive scope (line 131, DOCS-01):**
Old string:
```
- Scopes: `gmail.readonly`, `drive.file` (Phase 4+)
```
New string:
```
- Scopes: `gmail.readonly`, `drive.file`
```

**INTEGRATIONS.md Edit 1 — Remove "(Phase 4+)" from Drive scope (line 76, DOCS-01):**
Old string:
```
  - `https://www.googleapis.com/auth/drive.file` - Upload/manage Drive files (Phase 4+)
```
New string:
```
  - `https://www.googleapis.com/auth/drive.file` - Upload/manage Drive files
```

**INTEGRATIONS.md Edit 2 — Remove "(Phase 4)" from Drive client (line 82, DOCS-01):**
Old string:
```
  - Drive client: `job_finder/web/drive_uploader.py` (Phase 4)
```
New string:
```
  - Drive client: `job_finder/web/drive_uploader.py`
```

**INTEGRATIONS.md Edit 3 — Remove "(Phase 4)" from drive.folder_id config (line 163, DOCS-03):**
Old string:
```
- `drive.folder_id` - Google Drive folder for resume uploads (Phase 4)
```
New string:
```
- `drive.folder_id` - Google Drive folder for resume uploads
```

**INTEGRATIONS.md Edit 4 — Remove "(Phase 4, future)" from outgoing Drive reference (line 181, DOCS-03):**
Old string:
```
- Google Drive API: Resume file uploads (Phase 4, future)
```
New string:
```
- Google Drive API: Resume file uploads
```
  </action>
  <verify>
    <automated>cd <repo-root> && grep -c "Phase 4" .planning/codebase/STACK.md && grep -c "Phase 5" .planning/codebase/STACK.md && grep -c "Phase 4" .planning/codebase/INTEGRATIONS.md</automated>
    Expected output: 0, 0, 0 (zero phase annotations in both files)
  </verify>
  <acceptance_criteria>
    - `grep "Phase 4" .planning/codebase/STACK.md` returns 0 matches
    - `grep "Phase 5" .planning/codebase/STACK.md` returns 0 matches
    - `grep "Phase 4" .planning/codebase/INTEGRATIONS.md` returns 0 matches
    - `grep "Notifications:" .planning/codebase/STACK.md` returns 1 match (header without phase annotation)
    - `grep "drive.file" .planning/codebase/STACK.md` returns 1 match (scope without phase annotation)
    - `grep "Upload/manage Drive files" .planning/codebase/INTEGRATIONS.md` returns 1 match (no trailing annotation)
    - `grep "drive_uploader.py" .planning/codebase/INTEGRATIONS.md` returns 1 match (no trailing annotation)
    - `grep "Resume file uploads" .planning/codebase/INTEGRATIONS.md` returns 1 match ending without "(Phase 4, future)")
  </acceptance_criteria>
  <done>STACK.md and INTEGRATIONS.md have zero "(Phase N)" annotations — all features documented as current, not future</done>
</task>

<task type="auto">
  <name>Task 2: Final verification sweep across all planning docs</name>
  <files>N/A (read-only verification)</files>
  <read_first>
    - .planning/ROADMAP.md (verify Phase 4/5 status is correct — already updated)
    - CLAUDE.md (verify after Plan 01 edits, or verify current stale state if running in parallel)
    - .planning/PROJECT.md (verify after Plan 01 edits)
    - .planning/STATE.md (verify after Plan 01 edits)
    - .planning/codebase/STACK.md (verify after Task 1 edits)
    - .planning/codebase/INTEGRATIONS.md (verify after Task 1 edits)
  </read_first>
  <action>
Run verification commands to confirm all planning docs are clean. This task validates both Plan 01 and Plan 02 results.

**Verification 1 — Grep for stale "deferred" references (DOCS-03):**
```bash
grep -rn "deferred" .planning/ CLAUDE.md
```
Expected: Zero matches referencing Phase 4/5 as deferred. The only acceptable matches are in ROADMAP.md Phase 13 goal/success criteria (which describe what was fixed, not the stale state itself) and in the implementation plan under docs/superpowers/.

**Verification 2 — Grep for stale phase annotations (DOCS-01):**
```bash
grep -rn "Phase 4+" .planning/codebase/ CLAUDE.md
grep -rn "Phase 4)" .planning/codebase/ CLAUDE.md
grep -rn "Phase 5)" .planning/codebase/
```
Expected: Zero matches across all codebase docs and CLAUDE.md.

**Verification 3 — Grep for "Not started" on Phase 4/5 (DOCS-01):**
```bash
grep -rn "Not started" CLAUDE.md .planning/PROJECT.md .planning/STATE.md
```
Expected: Zero matches.

**Verification 4 — Confirm "Job Cannon" in CLAUDE.md (DOCS-04):**
```bash
grep "Job Cannon is a personal" CLAUDE.md
```
Expected: Exactly 1 match.

**Verification 5 — Confirm test count (DOCS-02):**
```bash
grep "1359" CLAUDE.md .planning/PROJECT.md
```
Expected: Matches in both files.

If any verification fails (because Plan 01 hasn't run yet in parallel execution), note which checks passed and which need re-verification after Plan 01 completes.
  </action>
  <verify>
    <automated>cd <repo-root> && echo "=== deferred ===" && grep -rn "deferred" .planning/PROJECT.md .planning/STATE.md CLAUDE.md | grep -v "Phase 13" | grep -v CONTEXT | wc -l && echo "=== Phase annotations ===" && grep -rn "Phase 4[+)]" .planning/codebase/ CLAUDE.md 2>/dev/null | wc -l && echo "=== Not started ===" && grep -rn "Not started" CLAUDE.md .planning/PROJECT.md .planning/STATE.md 2>/dev/null | wc -l</automated>
    Expected output: 0, 0, 0 for all three counts
  </verify>
  <acceptance_criteria>
    - `grep -rn "deferred" .planning/PROJECT.md .planning/STATE.md CLAUDE.md | grep -v "Phase 13" | grep -v CONTEXT` returns 0 matches
    - `grep -rn "Phase 4[+)]" .planning/codebase/ CLAUDE.md` returns 0 matches
    - `grep -rn "Phase 5)" .planning/codebase/` returns 0 matches
    - `grep -rn "Not started" CLAUDE.md .planning/PROJECT.md .planning/STATE.md` returns 0 matches
    - `grep "Job Cannon is a personal" CLAUDE.md` returns exactly 1 match
    - `grep "1359" CLAUDE.md .planning/PROJECT.md` returns matches in both files
  </acceptance_criteria>
  <done>All planning docs pass verification sweep — zero stale references to deferred phases, phase annotations, or incorrect stats across ROADMAP.md, PROJECT.md, STATE.md, CLAUDE.md, STACK.md, and INTEGRATIONS.md</done>
</task>

</tasks>

<verification>
Complete phase verification — run after both plans finish:
```bash
# Comprehensive stale reference check
grep -rn "deferred\|Not started\|Phase 4+\|Phase 4)\|Phase 5)\|will generate\|Job Finder" .planning/ CLAUDE.md 2>/dev/null | grep -v "docs/superpowers" | grep -v "CONTEXT.md" | grep -v "Phase 13"
```
Expected: Zero output. Any match indicates a missed edit.
</verification>

<success_criteria>
- STACK.md has zero "(Phase N)" annotations — DOCS-01
- INTEGRATIONS.md has zero "(Phase N)" or "(Phase N, future)" annotations — DOCS-01, DOCS-03
- Cross-file verification sweep passes with zero stale references — DOCS-03
- Test count "1359" appears in both CLAUDE.md and PROJECT.md — DOCS-02
</success_criteria>

<output>
After completion, create `.planning/phases/13-planning-doc-corrections/13-02-SUMMARY.md`
</output>
